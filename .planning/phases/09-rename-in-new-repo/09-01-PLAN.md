---
phase: 09-rename-in-new-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/manifest.json
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/const.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/__init__.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/config_flow.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/coordinator.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/sensor.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/select.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/number.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_select.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_number.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/__init__.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/__init__.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml
  - /home/dwolbeck/claude-code/luxtronik2-hass/README.md
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-03
  - RENAME-04
  - RENAME-05

must_haves:
  truths:
    - "custom_components/luxtronik2_hass/ exists in the working copy and custom_components/luxtronik2_modbus_proxy/ does not"
    - "manifest.json domain is luxtronik2_hass and name is 'Luxtronik 2.0 (Home Assistant)'"
    - "const.py DOMAIN constant equals luxtronik2_hass and all Python imports resolve against luxtronik2_hass"
    - "grep -r luxtronik2_modbus_proxy over tracked sources (py/json/toml/md excluding .git) returns zero hits"
    - "pytest passes on the renamed tree"
  artifacts:
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/manifest.json"
      provides: "HA integration manifest with new domain, name and URLs"
      contains: '"domain": "luxtronik2_hass"'
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/const.py"
      provides: "Python DOMAIN constant"
      contains: 'DOMAIN = "luxtronik2_hass"'
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml"
      provides: "Distribution package metadata renamed to luxtronik2-hass"
      contains: "luxtronik2-hass"
  key_links:
    - from: "tests/unit/test_select.py and tests/unit/test_number.py"
      to: "custom_components/luxtronik2_hass"
      via: "Python import statements"
      pattern: "custom_components\\.luxtronik2_hass"
    - from: "manifest.json documentation and issue_tracker URLs"
      to: "github.com/notDIRK/luxtronik2-hass"
      via: "JSON string values"
      pattern: "notDIRK/luxtronik2-hass"
---

<objective>
Rebrand the extracted HA integration working copy at `/home/dwolbeck/claude-code/luxtronik2-hass` from `luxtronik2_modbus_proxy` to `luxtronik2_hass` — folder, HA domain, Python imports, package metadata, URLs, and placeholder README — while keeping the existing pytest suite green. Phase 9 is local-only: no push, no network calls.

Purpose: Establish the HA-first identity in the new repo so Phase 10 can write documentation against stable names and Phase 11 can push a correctly-branded repo.
Output: A fully renamed working tree where `grep -r luxtronik2_modbus_proxy` over tracked source files returns zero and `pytest` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

# Working directory note
ALL implementation commands run in `/home/dwolbeck/claude-code/luxtronik2-hass`, NOT in this source repo.
The only files written inside this source repo are this PLAN.md and its eventual SUMMARY.md under `.planning/phases/09-rename-in-new-repo/`.

<rename_mapping>
# Exact string substitutions (case-sensitive)
luxtronik2_modbus_proxy          -> luxtronik2_hass         # Python package, HA domain, underscore form
luxtronik2-modbus-proxy          -> luxtronik2-hass         # PyPI/distribution, repo slug, hyphen form
"Luxtronik 2 Modbus Proxy"       -> "Luxtronik 2.0 (Home Assistant)"   # Manifest "name"
notDIRK/luxtronik2-modbus-proxy  -> notDIRK/luxtronik2-hass # GitHub repo slug in URLs

# DO NOT TOUCH
- "Luxtronik 2.0"  (product name of the heat pump controller)
- BACKUP_DIR = "luxtronik2_backups" in const.py (user-facing HA path, renaming would break existing installs)
- translations/*.json strings unless they literally contain luxtronik2_modbus_proxy or "Luxtronik 2 Modbus Proxy"
- git history / commit messages (historical log hits allowed)
</rename_mapping>

<target_manifest_json>
{
  "domain": "luxtronik2_hass",
  "name": "Luxtronik 2.0 (Home Assistant)",
  "version": "1.1.2",
  "integration_type": "hub",
  "iot_class": "local_polling",
  "requirements": ["luxtronik==0.3.14"],
  "codeowners": ["@notDIRK"],
  "documentation": "https://github.com/notDIRK/luxtronik2-hass",
  "issue_tracker": "https://github.com/notDIRK/luxtronik2-hass/issues",
  "config_flow": true
}
</target_manifest_json>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename folder + update manifest.json + const.py (core identity)</name>
  <files>
    /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/manifest.json,
    /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_hass/const.py
  </files>
  <read_first>
    - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_modbus_proxy/manifest.json (current values)
    - /home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_modbus_proxy/const.py (full file — check for DOMAIN and any other literal luxtronik2_modbus_proxy hits; preserve BACKUP_DIR = "luxtronik2_backups")
  </read_first>
  <action>
    Implements RENAME-01, RENAME-02, RENAME-03 (partial).

    1. `cd /home/dwolbeck/claude-code/luxtronik2-hass`
    2. Rename the folder preserving git history:
       `git mv custom_components/luxtronik2_modbus_proxy custom_components/luxtronik2_hass`
    3. Overwrite `custom_components/luxtronik2_hass/manifest.json` with EXACTLY the JSON from `<target_manifest_json>` in the plan context (domain=luxtronik2_hass, name="Luxtronik 2.0 (Home Assistant)", URLs point at notDIRK/luxtronik2-hass, version stays 1.1.2).
    4. Edit `custom_components/luxtronik2_hass/const.py`:
       - Replace `DOMAIN = "luxtronik2_modbus_proxy"` with `DOMAIN = "luxtronik2_hass"`.
       - Do NOT change `BACKUP_DIR = "luxtronik2_backups"` — this is a user-facing directory on the HA instance (per locked decision D-10).
       - If const.py contains any other literal `luxtronik2_modbus_proxy` strings (e.g. logger names, URL constants), replace with `luxtronik2_hass` / `luxtronik2-hass` per the rename_mapping table.
    5. Sweep the renamed package directory for any remaining literal `luxtronik2_modbus_proxy` in Python files (imports that used absolute package paths, explicit `logging.getLogger("luxtronik2_modbus_proxy...")`, etc.) and replace with `luxtronik2_hass`:
       `grep -rn luxtronik2_modbus_proxy custom_components/luxtronik2_hass/` — fix each hit.
       Standard `_LOGGER = logging.getLogger(__name__)` needs no change (resolves from module path).
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && test -d custom_components/luxtronik2_hass && ! test -e custom_components/luxtronik2_modbus_proxy && grep -q '"domain": "luxtronik2_hass"' custom_components/luxtronik2_hass/manifest.json && grep -q '"name": "Luxtronik 2.0 (Home Assistant)"' custom_components/luxtronik2_hass/manifest.json && grep -q 'notDIRK/luxtronik2-hass' custom_components/luxtronik2_hass/manifest.json && grep -q 'DOMAIN = "luxtronik2_hass"' custom_components/luxtronik2_hass/const.py && ! grep -rq luxtronik2_modbus_proxy custom_components/luxtronik2_hass/ && echo PASS</automated>
  </verify>
  <done>
    - `custom_components/luxtronik2_hass/` exists and `custom_components/luxtronik2_modbus_proxy/` does not.
    - manifest.json contains domain=luxtronik2_hass, name="Luxtronik 2.0 (Home Assistant)", and `notDIRK/luxtronik2-hass` in both documentation and issue_tracker URLs.
    - `const.py` has `DOMAIN = "luxtronik2_hass"` and retains `BACKUP_DIR = "luxtronik2_backups"`.
    - Zero `luxtronik2_modbus_proxy` hits inside `custom_components/luxtronik2_hass/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests, pyproject.toml, and README placeholder</name>
  <files>
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_select.py,
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_number.py,
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/__init__.py,
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/__init__.py,
    /home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml,
    /home/dwolbeck/claude-code/luxtronik2-hass/README.md
  </files>
  <read_first>
    - Run `cd /home/dwolbeck/claude-code/luxtronik2-hass && grep -rln 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --include='*.py' --include='*.toml' --include='*.md' --include='*.json' --include='*.cfg' --include='*.ini' --include='*.txt' --include='*.yml' --include='*.yaml' --exclude-dir=.git` to enumerate ALL remaining hits before editing.
    - Each file flagged by the grep above (read in full before editing).
  </read_first>
  <action>
    Implements RENAME-03 (tests), RENAME-04 (pyproject), RENAME-05 (URLs + strings).

    1. For each test file flagged by the grep sweep (expected: `tests/unit/test_select.py`, `tests/unit/test_number.py`, possibly `tests/unit/__init__.py`, `tests/__init__.py`):
       - Replace `luxtronik2_modbus_proxy` with `luxtronik2_hass` in import paths (e.g. `from custom_components.luxtronik2_modbus_proxy.select import ...` -> `from custom_components.luxtronik2_hass.select import ...`).
       - Replace any hyphen-form `luxtronik2-modbus-proxy` with `luxtronik2-hass`.
       - Leave all other code untouched.
    2. Edit `pyproject.toml`:
       - Distribution package name: `name = "luxtronik2-modbus-proxy"` -> `name = "luxtronik2-hass"` (hyphen form, PyPI convention; dev-only, not published).
       - Any `luxtronik2_modbus_proxy` Python package reference in `[tool.setuptools.packages.find]`, `[tool.ruff]`, `[tool.mypy]`, `[tool.pytest.ini_options]`, `[tool.coverage.*]`, etc. -> `luxtronik2_hass`.
       - Any URL fields (`homepage`, `repository`, `documentation`, `urls.*`) containing `notDIRK/luxtronik2-modbus-proxy` -> `notDIRK/luxtronik2-hass`.
       - Version stays `1.1.2` per locked decision.
    3. Edit `README.md`:
       - It is a Phase 8 placeholder; replace any `luxtronik2_modbus_proxy`, `luxtronik2-modbus-proxy`, or `Luxtronik 2 Modbus Proxy` occurrences with the new names per the rename_mapping table. Do not otherwise expand the README — full EN+DE rewrite is Phase 10's job.
    4. Run the exhaustive sweep again to catch anything missed:
       `cd /home/dwolbeck/claude-code/luxtronik2-hass && grep -rn 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --exclude-dir=.git`
       Exit code 1 (no matches) is required. If anything remains, fix it.
       Note: hits inside translations/*.json are only valid targets if they contain the literal `luxtronik2_modbus_proxy` or `Luxtronik 2 Modbus Proxy` — the bare "Luxtronik 2.0" product name MUST stay.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && ! grep -rq 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --exclude-dir=.git && grep -q 'luxtronik2-hass' pyproject.toml && echo PASS</automated>
  </verify>
  <done>
    - Tests import from `custom_components.luxtronik2_hass.*`.
    - pyproject.toml declares `name = "luxtronik2-hass"` and any internal package references use `luxtronik2_hass`.
    - README.md contains no old-name references.
    - Repo-wide grep for `luxtronik2_modbus_proxy`, `luxtronik2-modbus-proxy`, or `Luxtronik 2 Modbus Proxy` (excluding .git) returns zero hits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify renamed tree — full grep sweep + pytest + import smoke test</name>
  <files>
    (verification only — no files written)
  </files>
  <read_first>
    - None. This task only runs checks.
  </read_first>
  <action>
    Implements RENAME-01..05 verification (success criteria 1-4 of Phase 9).

    Run the following checks in sequence from `/home/dwolbeck/claude-code/luxtronik2-hass`. All must pass.

    1. Structural checks:
       ```
       test -d custom_components/luxtronik2_hass
       test ! -e custom_components/luxtronik2_modbus_proxy
       ```

    2. Manifest + const content checks:
       ```
       grep -q '"domain": "luxtronik2_hass"' custom_components/luxtronik2_hass/manifest.json
       grep -q '"name": "Luxtronik 2.0 (Home Assistant)"' custom_components/luxtronik2_hass/manifest.json
       grep -q '"documentation": "https://github.com/notDIRK/luxtronik2-hass"' custom_components/luxtronik2_hass/manifest.json
       grep -q '"issue_tracker": "https://github.com/notDIRK/luxtronik2-hass/issues"' custom_components/luxtronik2_hass/manifest.json
       grep -q 'DOMAIN = "luxtronik2_hass"' custom_components/luxtronik2_hass/const.py
       grep -q 'BACKUP_DIR = "luxtronik2_backups"' custom_components/luxtronik2_hass/const.py
       ```

    3. Full rename sweep (tracked files only — allow .git history):
       ```
       ! grep -rn 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --exclude-dir=.git
       ```
       Expected: exit code 1 (no matches).

    4. pytest:
       ```
       pytest
       ```
       If dependencies are missing, run `pip install -e .[dev]` or `pip install -e .` first as pyproject.toml dictates, then re-run. All tests that passed at end of Phase 8 MUST still pass.

    5. Import smoke test (bypasses HA runtime — verifies the package is importable as a Python package):
       ```
       python -c "import sys, importlib.util; sys.path.insert(0, 'custom_components'); spec = importlib.util.find_spec('luxtronik2_hass'); assert spec is not None, 'luxtronik2_hass package not found'; print('IMPORT OK')"
       ```
       Note: fully importing the module will fail because it `from homeassistant import ...`; `find_spec` is sufficient to prove the folder is a valid Python package at the new name. If the existing test suite already imports `custom_components.luxtronik2_hass.*` submodules successfully via pytest (Task 2 edits), that is stronger evidence and this smoke test can be skipped.

    6. If ANY check fails, STOP and report which check, which file, and the exact error — do not attempt to patch blindly.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && test -d custom_components/luxtronik2_hass && test ! -e custom_components/luxtronik2_modbus_proxy && grep -q '"domain": "luxtronik2_hass"' custom_components/luxtronik2_hass/manifest.json && grep -q 'DOMAIN = "luxtronik2_hass"' custom_components/luxtronik2_hass/const.py && ! grep -rq 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --exclude-dir=.git && pytest -q && echo PHASE9_VERIFIED</automated>
  </verify>
  <done>
    - All structural, content, and sweep checks in action step pass.
    - `pytest` exit code 0 with the same or greater number of passing tests than at end of Phase 8.
    - Import smoke test (or equivalent pytest evidence) confirms `luxtronik2_hass` is a valid Python package.
    - Terminal prints `PHASE9_VERIFIED`.
  </done>
</task>

</tasks>

<verification>
Phase 9 is verified when:
1. `custom_components/luxtronik2_hass/` exists; `custom_components/luxtronik2_modbus_proxy/` does not.
2. `grep -rn 'luxtronik2_modbus_proxy\|luxtronik2-modbus-proxy\|Luxtronik 2 Modbus Proxy' . --exclude-dir=.git` in the working copy returns zero hits.
3. `manifest.json` domain=luxtronik2_hass, name="Luxtronik 2.0 (Home Assistant)", URLs point at `notDIRK/luxtronik2-hass`; `const.py` `DOMAIN = "luxtronik2_hass"` with `BACKUP_DIR = "luxtronik2_backups"` preserved.
4. `pytest` passes on the renamed tree.
</verification>

<success_criteria>
- RENAME-01 ✅ manifest.json renamed (Task 1)
- RENAME-02 ✅ folder renamed via `git mv` (Task 1)
- RENAME-03 ✅ const.py DOMAIN + all Python imports (Tasks 1 + 2)
- RENAME-04 ✅ pyproject.toml distribution + package refs (Task 2)
- RENAME-05 ✅ internal strings, logger literals, URLs (Tasks 1 + 2, verified Task 3)
- All four Phase 9 Success Criteria verified green in Task 3
</success_criteria>

<output>
After completion, create `.planning/phases/09-rename-in-new-repo/09-01-SUMMARY.md` in the SOURCE repo `/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy` (NOT in the new working copy). Summarize:
- Exact files touched in `/home/dwolbeck/claude-code/luxtronik2-hass`
- `git log --oneline -5` from the new working copy showing the `git mv` + rename commits
- `pytest` result summary (N passed)
- Confirmation that `BACKUP_DIR = "luxtronik2_backups"` was preserved
- Any surprises or deviations from the rename_mapping table
</output>
