---
phase: 04-pypi-publish-hacs-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hacs.json
  - custom_components/luxtronik2_modbus_proxy/__init__.py
  - custom_components/luxtronik2_modbus_proxy/const.py
  - custom_components/luxtronik2_modbus_proxy/manifest.json
  - custom_components/luxtronik2_modbus_proxy/brand/icon.png
  - .github/workflows/validate.yml
autonomous: true
requirements:
  - HACS-01
  - HACS-03

must_haves:
  truths:
    - "hacs.json exists at repo root with correct name, homeassistant version, and render_readme"
    - "manifest.json has all required fields: domain, name, version, iot_class, requirements, codeowners, documentation, config_flow, integration_type"
    - "const.py defines DOMAIN, DEFAULT_PORT, DEFAULT_POLL_INTERVAL, MANUFACTURER, MODEL"
    - "__init__.py is a minimal stub (no integration logic)"
    - "brand/icon.png exists as a valid 256x256 PNG"
    - "validate.yml runs both hacs/action and hassfest on push and PR"
  artifacts:
    - path: "hacs.json"
      provides: "HACS repository metadata"
      contains: '"render_readme": true'
    - path: "custom_components/luxtronik2_modbus_proxy/manifest.json"
      provides: "HA integration manifest"
      contains: '"domain": "luxtronik2_modbus_proxy"'
    - path: "custom_components/luxtronik2_modbus_proxy/const.py"
      provides: "Integration constants"
      contains: "DOMAIN"
    - path: "custom_components/luxtronik2_modbus_proxy/__init__.py"
      provides: "Minimal stub for HA integration"
      min_lines: 1
    - path: "custom_components/luxtronik2_modbus_proxy/brand/icon.png"
      provides: "Brand icon for HA UI"
    - path: ".github/workflows/validate.yml"
      provides: "HACS + hassfest CI validation"
      contains: "hacs/action@main"
  key_links:
    - from: ".github/workflows/validate.yml"
      to: "hacs.json"
      via: "hacs/action validates hacs.json schema"
      pattern: "hacs/action@main"
    - from: ".github/workflows/validate.yml"
      to: "custom_components/luxtronik2_modbus_proxy/manifest.json"
      via: "hassfest validates manifest.json schema"
      pattern: "hassfest@master"
    - from: "custom_components/luxtronik2_modbus_proxy/manifest.json"
      to: "custom_components/luxtronik2_modbus_proxy/const.py"
      via: "Both define domain luxtronik2_modbus_proxy — must match"
      pattern: "luxtronik2_modbus_proxy"
---

<objective>
Create the HACS-discoverable integration skeleton: hacs.json at repo root, manifest.json with all required fields, const.py with domain constants, minimal __init__.py stub, brand icon, and the validate.yml CI workflow running both HACS and hassfest validation.

Purpose: HACS-01 requires valid hacs.json + manifest.json + brand icon. HACS-03 requires CI validation on every push. This scaffold is the foundation that Phases 5-7 build integration logic on top of.

Output: Complete `custom_components/luxtronik2_modbus_proxy/` directory with manifest, constants, stub, and brand icon; `hacs.json` at repo root; `.github/workflows/validate.yml` with two validation jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pypi-publish-hacs-scaffold/04-CONTEXT.md
@.planning/phases/04-pypi-publish-hacs-scaffold/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration skeleton (manifest, const, init, icon)</name>
  <files>
    hacs.json,
    custom_components/luxtronik2_modbus_proxy/__init__.py,
    custom_components/luxtronik2_modbus_proxy/const.py,
    custom_components/luxtronik2_modbus_proxy/manifest.json,
    custom_components/luxtronik2_modbus_proxy/brand/icon.png
  </files>
  <read_first>
    - .planning/phases/04-pypi-publish-hacs-scaffold/04-RESEARCH.md (Patterns 3, 4, 6 for exact schemas; Code Examples for const.py; Anti-Patterns for brand/ directory requirement)
    - .planning/phases/04-pypi-publish-hacs-scaffold/04-CONTEXT.md (D-06 through D-15 for locked decisions)
  </read_first>
  <action>
1. Create directory structure:
```
mkdir -p custom_components/luxtronik2_modbus_proxy/brand
```

2. Create `hacs.json` in the REPO ROOT (per D-07):
```json
{
  "name": "Luxtronik 2 Modbus Proxy",
  "homeassistant": "2024.1.0",
  "render_readme": true
}
```

3. Create `custom_components/luxtronik2_modbus_proxy/manifest.json` (per D-08, D-09, D-10, D-11):
```json
{
  "domain": "luxtronik2_modbus_proxy",
  "name": "Luxtronik 2 Modbus Proxy",
  "version": "1.1.0",
  "integration_type": "hub",
  "iot_class": "local_polling",
  "requirements": ["luxtronik==0.3.14"],
  "codeowners": ["@OWNER"],
  "documentation": "https://github.com/OWNER/luxtronik2-modbus-proxy",
  "issue_tracker": "https://github.com/OWNER/luxtronik2-modbus-proxy/issues",
  "config_flow": false
}
```
NOTE: Replace `@OWNER` and `OWNER` with the actual GitHub username. Determine by running `git remote get-url origin` or reading `.git/config`. If no remote is configured, use `OWNER` as placeholder and flag for human substitution. The `codeowners` and `documentation` fields are REQUIRED by HACS validation (Pitfall 6). `config_flow: false` is correct for this stub phase; Phase 5 changes it to `true`.

4. Create `custom_components/luxtronik2_modbus_proxy/const.py` (per D-12):
```python
"""Constants for the Luxtronik 2 Modbus Proxy integration."""

DOMAIN = "luxtronik2_modbus_proxy"

# Default connection parameters
DEFAULT_PORT = 8889
DEFAULT_POLL_INTERVAL = 30  # seconds

# Device identification
MANUFACTURER = "Alpha Innotec / Novelan"
MODEL = "Luxtronik 2.0"
```

5. Create `custom_components/luxtronik2_modbus_proxy/__init__.py` as a minimal stub (per D-13):
```python
"""Luxtronik 2 Modbus Proxy integration."""
```
No imports, no setup functions — Phase 5 adds async_setup_entry and platform logic.

6. Create brand icon at `custom_components/luxtronik2_modbus_proxy/brand/icon.png` (per D-14, D-15):
Generate a 256x256 PNG heat pump silhouette icon programmatically using Python with the `Pillow` library (or `PIL`). If Pillow is not available, install it temporarily: `pip install Pillow`.

The icon should be:
- 256x256 pixels, PNG format
- Monochrome (dark gray on transparent background)
- Simple heat pump silhouette: a rectangle body with radiator fins on one side and a circular fan on the other
- Clean, recognizable at small sizes (64x64 in HA sidebar)

Use this approach:
```python
from PIL import Image, ImageDraw

img = Image.new("RGBA", (256, 256), (0, 0, 0, 0))
draw = ImageDraw.Draw(img)

# Main body - rounded rectangle representing heat pump unit
draw.rounded_rectangle([48, 56, 208, 200], radius=12, fill=(80, 80, 80, 255))

# Fan circle
draw.ellipse([100, 88, 180, 168], outline=(200, 200, 200, 255), width=4)

# Fan blades (simplified as lines through center)
cx, cy = 140, 128
for angle_offset in [0, 60, 120]:
    import math
    rad = math.radians(angle_offset)
    x1 = cx + int(30 * math.cos(rad))
    y1 = cy + int(30 * math.sin(rad))
    x2 = cx - int(30 * math.cos(rad))
    y2 = cy - int(30 * math.sin(rad))
    draw.line([(x1, y1), (x2, y2)], fill=(200, 200, 200, 255), width=3)

# Base/stand
draw.rectangle([72, 200, 96, 224], fill=(80, 80, 80, 255))
draw.rectangle([160, 200, 184, 224], fill=(80, 80, 80, 255))

img.save("custom_components/luxtronik2_modbus_proxy/brand/icon.png")
```

Adjust the drawing to produce a clean, recognizable result. The exact design is Claude's discretion as long as it represents a heat pump and is clean/monochrome.
  </action>
  <verify>
    <automated>test -f hacs.json && test -f custom_components/luxtronik2_modbus_proxy/manifest.json && test -f custom_components/luxtronik2_modbus_proxy/const.py && test -f custom_components/luxtronik2_modbus_proxy/__init__.py && test -f custom_components/luxtronik2_modbus_proxy/brand/icon.png && python3 -c "import json; m=json.load(open('custom_components/luxtronik2_modbus_proxy/manifest.json')); assert m['domain']=='luxtronik2_modbus_proxy'; assert m['version']=='1.1.0'; assert m['iot_class']=='local_polling'; assert m['requirements']==['luxtronik==0.3.14']; assert 'config_flow' in m; h=json.load(open('hacs.json')); assert h['render_readme']==True; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `hacs.json` exists at repo root containing `"name": "Luxtronik 2 Modbus Proxy"`, `"homeassistant": "2024.1.0"`, `"render_readme": true`
    - `custom_components/luxtronik2_modbus_proxy/manifest.json` exists with `"domain": "luxtronik2_modbus_proxy"`, `"name": "Luxtronik 2 Modbus Proxy"`, `"version": "1.1.0"`, `"iot_class": "local_polling"`, `"requirements": ["luxtronik==0.3.14"]`, `"config_flow": false`, `"integration_type": "hub"`, and `"codeowners"` and `"documentation"` fields present
    - `custom_components/luxtronik2_modbus_proxy/const.py` contains `DOMAIN = "luxtronik2_modbus_proxy"`, `DEFAULT_PORT = 8889`, `DEFAULT_POLL_INTERVAL = 30`, `MANUFACTURER = "Alpha Innotec / Novelan"`, `MODEL = "Luxtronik 2.0"`
    - `custom_components/luxtronik2_modbus_proxy/__init__.py` exists and does NOT contain `async_setup_entry` (stub only)
    - `custom_components/luxtronik2_modbus_proxy/brand/icon.png` exists and is a valid PNG file (`python3 -c "from PIL import Image; img=Image.open('custom_components/luxtronik2_modbus_proxy/brand/icon.png'); assert img.size==(256,256); assert img.format=='PNG'"`)
  </acceptance_criteria>
  <done>All 5 integration skeleton files exist with correct content; hacs.json at repo root; icon is 256x256 PNG</done>
</task>

<task type="auto">
  <name>Task 2: Create HACS + hassfest validation workflow</name>
  <files>.github/workflows/validate.yml</files>
  <read_first>
    - .github/workflows/docs.yml (existing workflow pattern)
    - .planning/phases/04-pypi-publish-hacs-scaffold/04-RESEARCH.md (Pattern 2: HACS + Hassfest Validation Workflow; Pitfall 3: hacs/action does not check out code)
  </read_first>
  <action>
Create `.github/workflows/validate.yml` with the following exact content (per D-16, D-17, D-18):

```yaml
name: Validate

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  validate-hacs:
    runs-on: ubuntu-latest
    steps:
      - name: HACS validation
        uses: "hacs/action@main"
        with:
          category: "integration"

  validate-hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: "actions/checkout@v4"
      - uses: "home-assistant/actions/hassfest@master"
```

Key points enforced:
- `validate-hacs` job does NOT include `actions/checkout` — hacs/action handles checkout internally (Pitfall 3)
- `validate-hassfest` job DOES include `actions/checkout@v4` — hassfest requires explicit checkout
- Triggers on push to main AND pull_request (per D-17)
- Also includes daily schedule and workflow_dispatch for proactive validation
- `permissions: {}` at workflow level — principle of least privilege; neither action needs special permissions
- Two independent jobs run in parallel
  </action>
  <verify>
    <automated>test -f .github/workflows/validate.yml && grep -q 'hacs/action@main' .github/workflows/validate.yml && grep -q 'hassfest@master' .github/workflows/validate.yml && grep -q 'category: "integration"' .github/workflows/validate.yml && grep -q 'pull_request' .github/workflows/validate.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/validate.yml` exists
    - File contains `hacs/action@main` with `category: "integration"` input
    - File contains `home-assistant/actions/hassfest@master`
    - File contains `actions/checkout@v4` only in the `validate-hassfest` job (NOT in validate-hacs)
    - File triggers on `push: branches: - main` and `pull_request`
    - File has `permissions: {}` at workflow level
  </acceptance_criteria>
  <done>validate.yml exists with both hacs/action and hassfest validation, triggers on push to main and PRs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> HACS/HA validation | Third-party actions execute in CI; `@main`/`@master` tags float |
| HACS -> integration directory | HACS reads hacs.json and manifest.json to validate compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | validate.yml uses floating `@main`/`@master` tags | accept | Official HA and HACS actions; pinning to SHA would break auto-updates; risk is low — these are the actions HACS itself recommends. Monitor for upstream compromises. |
| T-04-06 | Information Disclosure | manifest.json codeowners exposes GitHub username | accept | GitHub username is already public on the repository; no additional exposure |
| T-04-07 | Denial of Service | Daily scheduled workflow | accept | One daily run is within GitHub Actions free tier; no cost risk |
</threat_model>

<verification>
1. `test -f hacs.json` — HACS manifest at repo root
2. `python3 -c "import json; json.load(open('hacs.json'))"` — valid JSON
3. `test -f custom_components/luxtronik2_modbus_proxy/manifest.json` — HA manifest exists
4. `python3 -c "import json; m=json.load(open('custom_components/luxtronik2_modbus_proxy/manifest.json')); assert m['domain']=='luxtronik2_modbus_proxy'"` — domain correct
5. `grep -q 'DOMAIN = "luxtronik2_modbus_proxy"' custom_components/luxtronik2_modbus_proxy/const.py` — const matches manifest
6. `test -f custom_components/luxtronik2_modbus_proxy/brand/icon.png` — icon exists
7. `test -f .github/workflows/validate.yml` — CI workflow exists
8. `grep -q 'hacs/action@main' .github/workflows/validate.yml` — HACS validation present
9. `grep -q 'hassfest@master' .github/workflows/validate.yml` — hassfest validation present
</verification>

<success_criteria>
- hacs.json at repo root is valid JSON with name, homeassistant, render_readme
- manifest.json has all HACS-required fields (domain, name, version, iot_class, requirements, codeowners, documentation, config_flow, integration_type)
- const.py defines DOMAIN matching manifest.json domain
- __init__.py is a stub with no integration logic
- brand/icon.png is a 256x256 PNG
- validate.yml runs hacs/action and hassfest on push to main and PRs
</success_criteria>

<output>
After completion, create `.planning/phases/04-pypi-publish-hacs-scaffold/04-02-SUMMARY.md`
</output>
