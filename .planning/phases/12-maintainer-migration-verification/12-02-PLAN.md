---
phase: 12-maintainer-migration-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md
  - /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md
autonomous: false
requirements:
  - MIGRATE-01
  - MIGRATE-02
  - MIGRATE-03
must_haves:
  truths:
    - "Maintainer has run the 7-step checklist on the live HA 2026.4.1 instance"
    - "Old luxtronik2_modbus_proxy config entry is deleted with zero ghost entities (MIGRATE-01)"
    - "New luxtronik2_hass integration is installed via HACS custom repository and config flow completed (MIGRATE-02)"
    - "31 entities under luxtronik_2_0_* and dashboard renders all 32 values (MIGRATE-03)"
    - "12-VERIFICATION.md records maintainer outcomes and flips status to PASSED or FAILED"
    - "MIGRATION.md in the new repo has a 'Maintainer verified' supplement with HA version, date, outcome"
  artifacts:
    - path: ".planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md"
      provides: "Populated Maintainer Action Log + Post-Migration Record, final status PASSED/FAILED"
      contains: "## Post-Migration Record"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md"
      provides: "Appended 'Maintainer verified' note"
      contains: "Maintainer verified"
  key_links:
    - from: "12-VERIFICATION.md Post-Migration Record"
      to: "MIGRATION.md Maintainer verified note"
      via: "same HA version + date + outcome text, copied in Task 3"
      pattern: "Maintainer verified.*2026-04"
---

<objective>
Capture the maintainer's live-migration results and close Phase 12.

Purpose: Plan 01 produced the checklist and pre-flight artifacts. Plan 02 is the human-in-the-loop execution phase: the maintainer runs the 7-step checklist against the live HA 2026.4.1 instance, Claude captures the results into `12-VERIFICATION.md`, then appends a "Maintainer verified" note to the new repo's `MIGRATION.md`. This plan is non-autonomous — Task 1 pauses for maintainer input.

Output:
- `12-VERIFICATION.md` fully populated: Maintainer Action Log filled, Post-Migration Record filled, status flipped to PASSED or FAILED, any bugs filed as follow-ups per D-03.
- `MIGRATION.md` in the new repo supplemented with a short "Maintainer verified" paragraph.
- Phase 12 closed; MIGRATE-01/02/03 accepted (or issues documented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-maintainer-migration-verification/12-CONTEXT.md
@.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md
@.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md
@.planning/phases/11-publish-archive-legacy/11-VERIFICATION.md
@CLAUDE.md

**Dependency:** Plan 01 must be complete. Pre-flight Check section of `12-VERIFICATION.md` must be marked PASS before Task 1 starts.

**CONTEXT.md decisions honored:**
- D-01/D-02: Maintainer executes manually. Claude does NOT call the HA API, WS, or REST. Claude does NOT run backup/rollback.
- D-03: Bugs found → filed as follow-ups in `12-VERIFICATION.md`, not fixed in Phase 12.
- D-05: Dashboard render is the single acceptance signal for MIGRATE-03.
- D-08: Two outputs — (1) `12-VERIFICATION.md` populated, (2) `MIGRATION.md` in new repo supplemented.
- D-09: Text-only. No screenshots.
- CLAUDE.md: PUBLIC repo. No real IPs/hostnames/credentials in any artifact. Use `your-heatpump-ip` placeholder.

**New repo path:** `/home/dwolbeck/claude-code/luxtronik2-hass/` (confirmed in Plan 01 context).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Maintainer executes migration checklist on live HA instance</name>
  <read_first>
    - .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md (the 7-step checklist)
    - .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md (pre-flight section — confirm GREEN before starting)
  </read_first>
  <what-built>
    Nothing automated. The prior steps produced a checklist, a dashboard in the new repo, and a verification skeleton. This checkpoint pauses execution so the maintainer can manually run the 7 steps in `12-HUMAN-UAT.md` against their live HA 2026.4.1 instance. Claude CANNOT automate this (per D-01, D-02 — explicit user decision, not a capability gap).
  </what-built>
  <how-to-verify>
    The maintainer:

    1. Opens `.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md` in an editor.
    2. Executes each of the 7 numbered steps in order on the live HA instance.
    3. Records the outcome of each step (OK or a short failure description).
    4. Checks entity count under `luxtronik_2_0_*` (expected: 31).
    5. Loads the Wärmepumpe dashboard in Lovelace and counts tiles that render vs. tiles showing "entity not found" / `unknown` / `unavailable` (expected: all 32 render).
    6. Notes the HA version and execution date.
    7. Returns to this session with the results in the form:

       ```
       HA version: <e.g. 2026.4.1>
       Date: <YYYY-MM-DD>
       Step 1: <OK | details>
       Step 2: <OK | details>
       Step 3: <OK | details>
       Step 4: <OK | details>
       Step 5: <OK | details>
       Step 6: <OK | details>
       Step 7: <OK | details>
       Entity count under luxtronik_2_0_*: <N>
       Dashboard render: <all 32 OK | list of failing entities>
       Bugs to file as follow-ups: <none | brief list>
       Overall outcome: <PASSED | FAILED>
       ```

       Do NOT paste real IPs or hostnames in the report — Claude will reject and re-ask if real network data appears.
  </how-to-verify>
  <resume-signal>
    Maintainer pastes the results block above (or types "failed: <reason>" to abort Phase 12).
  </resume-signal>
  <acceptance_criteria>
    - Maintainer has provided a results block containing: HA version, date, outcome per step 1-7, entity count, dashboard render result, follow-up bug list, overall outcome.
    - Results block contains no real IPs (no `192.168.*` pattern) and no real hostnames (`.local`, `.net` in a hostname-looking string).
    - Overall outcome is one of: `PASSED`, `FAILED`.
  </acceptance_criteria>
  <files>(none — this is a human-action checkpoint; no files are written by Claude in this task)</files>
  <action>PAUSE execution. Present the contents of `.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md` to the maintainer. Wait for the maintainer to execute the 7 checklist steps manually against their live HA 2026.4.1 instance and paste back the results block defined in <how-to-verify>. Do NOT proceed to Task 2 until the maintainer has pasted results AND the results block passes placeholder-safety (no real IPs, no real hostnames). If the pasted results contain real network data, reject and ask the maintainer to re-send sanitized. Claude performs NO HA API calls, NO automation, NO backup, NO rollback (per D-01, D-02).</action>
  <verify>
    <automated>MANUAL — this is a human-action checkpoint. Verification is the maintainer's pasted results block matching the shape in &lt;how-to-verify&gt;, scanned for `192.168.*` patterns (must be absent).</automated>
  </verify>
  <done>Maintainer has pasted a sanitized results block with HA version, date, per-step outcomes, entity count, dashboard render result, bug list, and overall outcome (PASSED or FAILED).</done>
</task>

<task type="auto">
  <name>Task 2: Populate 12-VERIFICATION.md with maintainer results</name>
  <files>.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md (current skeleton from Plan 01)
    - .planning/phases/11-publish-archive-legacy/11-VERIFICATION.md (formatting reference — how to present PASSED results)
    - The maintainer results block from Task 1
  </read_first>
  <action>
    Edit `.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md` to populate the TBD sections with the maintainer's results from Task 1.

    Specific edits:

    1. **Frontmatter line `**Date:**`:** Replace `/ TBD (maintainer execution)` with the actual execution date from the maintainer report (e.g. `/ 2026-04-10 (maintainer execution)`).

    2. **Status line:** Change `**Status:** IN PROGRESS — pre-flight complete, awaiting maintainer execution` to either:
       - `**Status:** PASSED — all 3 MIGRATE requirements satisfied on live HA <version>` (if overall outcome = PASSED), or
       - `**Status:** FAILED — <brief reason>; see Post-Migration Record for details` (if overall outcome = FAILED).

    3. **Maintainer Action Log table:** Replace each row's `TBD | TBD` with the actual timestamp (use the maintainer's execution date + "manual run" if no precise time given) and outcome (literal string from the maintainer report — `OK` or the failure description).

    4. **Post-Migration Record bullets:** Replace each `TBD` with the concrete value from the maintainer report:
       - `**HA version:**` → actual version
       - `**Date of maintainer execution:**` → actual date
       - `**Old domain ghost entities:**` → `0` (if MIGRATE-01 passed) or actual count
       - `**New integration config flow:**` → `completed without errors` (if MIGRATE-02 passed) or failure description
       - `**Entity count under luxtronik_2_0_*:**` → actual count from maintainer report
       - `**Dashboard render:**` → `all 32 values render, zero errors` (if MIGRATE-03 passed) or list of failing entities
       - `**Bugs/issues surfaced:**` → literal list from maintainer report, or `None` if empty. For each bug, add a line `- TODO: file issue — <description> (not fixed in Phase 12 per D-03)`.

    5. **Success Criteria section:** Replace each `**TBD**` with `**PASS**` or `**FAIL — <reason>**` per MIGRATE-01/02/03.

    6. **Overall status line at the bottom:** Change `IN PROGRESS — will flip to PASSED/FAILED in Plan 02.` to either `**PASSED** — Phase 12 complete. All 3 MIGRATE requirements satisfied.` or `**FAILED** — <brief reason>. Follow-up work required.`.

    **Security check before writing:** Scan the maintainer's results block for any `192.168.*`, `10.*`, real hostnames, PINs/passwords. If found, sanitize to `your-heatpump-ip` / `[redacted]` before writing into the file. Fail loudly if sanitization isn't possible without losing meaning — in that case, pause and ask the maintainer to re-send a sanitized block.

    Do NOT add any section not present in the skeleton. Do NOT delete the Requirements Covered section or the Pre-flight Check section (Plan 01's output is preserved as-is).
  </action>
  <verify>
    <!-- TIMING NOTE: This automated check runs ONLY after Task 1's human-action gate has been satisfied and this task has written the maintainer's results into 12-VERIFICATION.md. The skeleton produced by Plan 01 Task 3 legitimately contains TBD tokens until this task populates them — running this verify before Task 2 executes would (correctly) fail. Execution order is enforced by the task sequence: Task 1 (human gate) → Task 2 (populate + this verify). -->
    <automated>test -f .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && ! grep -q 'TBD' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -qE '\*\*Status:\*\* (PASSED|FAILED)' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-01' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-02' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-03' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && ! grep -qE '192\.168\.[0-9]+\.[0-9]+' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero `TBD` strings remain in the file
    - Status line matches exactly one of `**Status:** PASSED` or `**Status:** FAILED`
    - All three requirement IDs (MIGRATE-01, MIGRATE-02, MIGRATE-03) still present
    - No real IP addresses (`192.168.*`) in the file
    - Pre-flight Check section from Plan 01 is preserved unchanged
    - Maintainer Action Log has 7 populated rows (one per checklist step)
  </acceptance_criteria>
  <done>
    VERIFICATION.md fully populated with maintainer results, status flipped, placeholder-safe.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append "Maintainer verified" note to MIGRATION.md in new repo</name>
  <files>/home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md</files>
  <read_first>
    - /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md (current content — 110 lines; final section is "Why the Rename?")
    - .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md (freshly populated Post-Migration Record — source of HA version, date, outcome)
    - .planning/phases/12-maintainer-migration-verification/12-CONTEXT.md (D-08 — this is an append-only supplement)
  </read_first>
  <action>
    Append a new section to the END of `/home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md`. Do NOT modify any existing content — this is a pure append.

    Exact append content (starts with a blank line, then the new section heading):

    ```

    ## Maintainer Verified

    **HA version:** <VERSION_FROM_VERIFICATION_MD>
    **Date:** <DATE_FROM_VERIFICATION_MD>
    **Outcome:** <OUTCOME_SENTENCE>

    The maintainer walked the full migration path above on their own Home Assistant instance: deleted the old `luxtronik2_modbus_proxy` config entry, removed the old HACS custom repository, added `notDIRK/luxtronik2-hass` as a new HACS custom repository, installed the integration, restarted HA, re-entered the heat pump IP in the config flow, and confirmed the Wärmepumpe dashboard (`docs/examples/dashboard-waermepumpe.yaml`) renders all 32 referenced values under the stable `luxtronik_2_0_*` device slug.

    <OUTCOME_DETAIL_PARAGRAPH>
    ```

    Substitute the placeholders with values from `12-VERIFICATION.md` Post-Migration Record:
    - `<VERSION_FROM_VERIFICATION_MD>` → e.g. `2026.4.1`
    - `<DATE_FROM_VERIFICATION_MD>` → e.g. `2026-04-10`
    - `<OUTCOME_SENTENCE>` → if PASSED: `Migration completed cleanly — all 31 entities re-appeared under the stable \`luxtronik_2_0_*\` slug and the dashboard rendered without errors.` If FAILED: `Migration encountered issues — see follow-up bug list below.`
    - `<OUTCOME_DETAIL_PARAGRAPH>`:
      - If PASSED with no bugs: `No follow-up bugs were filed.`
      - If PASSED with minor bugs: `Follow-up bugs filed (not blockers):` followed by a bulleted list copied from the VERIFICATION.md follow-up list.
      - If FAILED: `The following issues were encountered and are tracked as follow-up work:` followed by a bulleted list.

    **Security scan before writing:** grep the final append text for `192.168.*`, real hostnames, PINs. If any real network data leaks through from the VERIFICATION.md source, sanitize to `your-heatpump-ip` before writing. This file will be pushed to a PUBLIC GitHub repo by the maintainer later.

    **Verification after append:**
    1. Line count should be original + N (where N = ~10-20 new lines)
    2. First 110 lines must be byte-identical to the original (use `head -110 MIGRATION.md | diff -q - <backup>` if needed — or simpler: `grep -q "^## Why the Rename?"` still matches and is not the last section anymore)
    3. `grep -c "Maintainer Verified" MIGRATION.md` returns exactly `1`
    4. No `192.168.*` patterns

    Do NOT stage or commit the file in the new repo. Leave it as an unstaged modification. The maintainer will review and push separately.
  </action>
  <verify>
    <automated>test -f /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md && [ "$(grep -c 'Maintainer Verified' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md)" = "1" ] && grep -q '^## Maintainer Verified' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md && grep -q 'luxtronik_2_0_' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md && grep -q 'dashboard-waermepumpe.yaml' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md && ! grep -qE '192\.168\.[0-9]+\.[0-9]+' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md && grep -q '^## Why the Rename' /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Maintainer Verified` heading appears exactly once at the end of `MIGRATION.md`
    - Contains the literal strings `HA version:`, `Date:`, `Outcome:`, `luxtronik_2_0_`, `dashboard-waermepumpe.yaml`
    - Original `## Why the Rename?` section is preserved (not deleted or reordered)
    - No `192.168.*` patterns anywhere in the file
    - File in new repo working tree is modified (unstaged) — NOT committed, NOT pushed
    - Only `MIGRATION.md` + the Plan-01-created `docs/examples/dashboard-waermepumpe.yaml` appear in `git status` of the new repo; nothing else is touched
  </acceptance_criteria>
  <done>
    MIGRATION.md has a "Maintainer Verified" supplement with HA version, date, and outcome. New repo working tree contains exactly 2 changes (the dashboard from Plan 01 and this MIGRATION.md append).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Phase 12 artifacts | Maintainer-pasted results could contain real network data (IPs, hostnames, PINs). Must be sanitized before write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | Maintainer-pasted results → 12-VERIFICATION.md | mitigate | Task 2 explicitly grep-scans for `192.168.*` before writing; acceptance criteria rejects any match. Pre-commit hook provides second line of defense. |
| T-12-04 | Information Disclosure | Maintainer-pasted results → MIGRATION.md (new repo, PUBLIC) | mitigate | Task 3 sanitizes before append; acceptance criteria rejects `192.168.*`. New repo will be pushed by maintainer, not Claude — human review is the third gate. |
| T-12-05 | Tampering | Append to MIGRATION.md in new repo | mitigate | Task 3 is append-only and verifies the original `## Why the Rename?` section still exists post-append. No in-place edits to existing content. |
</threat_model>

<verification>
- Phase 12 closes when VERIFICATION.md has no TBDs, status is PASSED or FAILED, and MIGRATION.md has the Maintainer Verified note.
- If outcome = FAILED: Phase 12 is marked FAILED in VERIFICATION.md, but still closes — follow-up bugs are filed as separate work (D-03). Do NOT loop back to fix bugs inside Phase 12.
- No git commits in the new repo; maintainer handles push.
- Commit the two legacy-repo files (`12-VERIFICATION.md` populated + plan artifacts) via the standard plan-close commit.
</verification>

<success_criteria>
- **MIGRATE-01:** Maintainer confirms old `luxtronik2_modbus_proxy` config entry deleted with zero ghost entities. Recorded in VERIFICATION.md with **PASS** or **FAIL**.
- **MIGRATE-02:** Maintainer confirms new `luxtronik2_hass` installed via HACS custom repository `notDIRK/luxtronik2-hass` and config flow completed. Recorded in VERIFICATION.md with **PASS** or **FAIL**.
- **MIGRATE-03:** Maintainer confirms 31 entities under `luxtronik_2_0_*` and dashboard renders all 32 values without errors. Recorded in VERIFICATION.md with **PASS** or **FAIL**.
- `12-VERIFICATION.md` contains zero `TBD` strings.
- `/home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md` contains exactly one `## Maintainer Verified` section.
- Both files pass a placeholder-safety grep (no real IPs/hostnames).
- `git status` in the new repo shows exactly 2 modifications: `docs/examples/dashboard-waermepumpe.yaml` (new, from Plan 01) and `MIGRATION.md` (modified, from this plan). Nothing else.
</success_criteria>

<output>
After completion, create `.planning/phases/12-maintainer-migration-verification/12-02-SUMMARY.md`.
</output>
