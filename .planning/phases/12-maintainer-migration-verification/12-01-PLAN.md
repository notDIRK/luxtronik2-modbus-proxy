---
phase: 12-maintainer-migration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml
  - .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md
  - .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02
  - MIGRATE-03
must_haves:
  truths:
    - "Dashboard YAML exists in the new luxtronik2-hass repo"
    - "Dashboard references only stable luxtronik_2_0_* entity IDs (no luxtronik2_modbus_proxy_* strings)"
    - "Maintainer has a numbered, copy-pasteable 7-step migration checklist"
    - "VERIFICATION.md skeleton exists with all required sections for Plan 02 to fill in"
  artifacts:
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml"
      provides: "Dashboard mirrored into new repo"
      contains: "luxtronik_2_0_"
    - path: ".planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md"
      provides: "Maintainer migration checklist (7 numbered steps)"
      contains: "^1\\. "
    - path: ".planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md"
      provides: "Audit trail skeleton with Pre-flight Check / Maintainer Action Log / Post-Migration Record sections"
      contains: "## Pre-flight Check"
  key_links:
    - from: "12-HUMAN-UAT.md"
      to: "docs/examples/dashboard-waermepumpe.yaml (new repo)"
      via: "step 7 instructs maintainer to open this dashboard"
      pattern: "dashboard-waermepumpe.yaml"
---

<objective>
Prepare pre-flight artifacts for the maintainer's live HA migration to `luxtronik2_hass`.

Purpose: Plan 02 (maintainer execution) needs (a) a clean dashboard in the new repo to verify against, (b) a numbered checklist the maintainer can follow step by step, and (c) a verification skeleton to fill in with results. All three are produced autonomously by Claude without touching HA.

Output:
- Dashboard copied into the new repo with line 11 sanitized (real IP+hostname → `your-heatpump-ip`) and grep verification of entity-ID stability + placeholder safety.
- `12-HUMAN-UAT.md` — 7-step copy-pasteable checklist.
- `12-VERIFICATION.md` — audit-trail skeleton for Plan 02 to fill in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-maintainer-migration-verification/12-CONTEXT.md
@.planning/phases/11-publish-archive-legacy/11-VERIFICATION.md
@docs/examples/dashboard-waermepumpe.yaml
@CLAUDE.md

**Key facts (measured 2026-04-10):**
- Live HA: version 2026.4.1, Supervised, maintainer's own instance.
- Expected entity count post-migration: 31 under `luxtronik_2_0_*` prefix.
- Dashboard references: 32 values, all using `luxtronik_2_0_*` (already verified clean: `grep -c luxtronik2_modbus_proxy docs/examples/dashboard-waermepumpe.yaml` returns 0, `grep -c luxtronik_2_0_` returns 32).
- New repo path on disk: `/home/dwolbeck/claude-code/luxtronik2-hass/` (NOT `PUBLIC-luxtronik2-hass/` — context doc had wrong path; confirmed by `ls` showing `custom_components/`, `MIGRATION.md`, etc.).
- New repo is published/live. Phase 12 is allowed to (a) create `docs/examples/dashboard-waermepumpe.yaml` there (copy + verify, per D-07), and (b) in Plan 02 only, append to `MIGRATION.md` (per D-08). No other touches.

**CONTEXT.md decisions honored:**
- D-01/D-02: No automation, no HA API, no backup, no rollback.
- D-03: Bugs found → follow-up, not Phase 12.
- D-04/D-05: Dashboard must render post-migration; this is the acceptance signal for MIGRATE-03.
- D-06: Exactly 7 numbered checklist steps.
- D-07: Copy dashboard into new repo if missing, grep-verify entity-ID stability.
- D-08: Plan 01 only creates the VERIFICATION.md skeleton; Plan 02 fills it and writes the MIGRATION.md note.
- D-09: Text-only record, no screenshots.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror dashboard into new repo and verify entity-ID stability</name>
  <files>/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml</files>
  <read_first>
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/docs/examples/dashboard-waermepumpe.yaml (source of truth)
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/12-maintainer-migration-verification/12-CONTEXT.md (D-04, D-07)
    - Run `ls /home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/ 2>&1` to confirm whether the target already exists (currently does NOT — `docs/examples/` directory does not exist in the new repo as of 2026-04-10).
  </read_first>
  <action>
    **Security note (MANDATORY):** The source dashboard file's line 11 contains a real IP (`192.168.100.186`) and a real hostname (`waermepumpe.intranet.wolbeck.net`). The target repo `luxtronik2-hass` is a **NEW PUBLIC GitHub repo** and this file does NOT yet exist there — writing it unmodified would be a **first-time publication** of real network data into a public repo. CLAUDE.md §SECURITY forbids this absolutely. **Sanitize line 11 during copy.** The legacy-repo copy staying as-is per D-03 is a separate, pre-existing issue tracked outside Phase 12 — that exemption does NOT apply here.

    1. Create directory if missing:
       `mkdir -p /home/dwolbeck/claude-code/luxtronik2-hass/docs/examples`
    2. Copy the dashboard file with line 11 sanitized in the same step. Use `sed` (exact command):
       ```
       sed 's|\*\*Host:\*\* 192\.168\.100\.186 (waermepumpe\.intranet\.wolbeck\.net) · \*\*Port:\*\* 8889|**Host:** your-heatpump-ip · **Port:** 8889|' \
         /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/docs/examples/dashboard-waermepumpe.yaml \
         > /home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml
       ```
       This replaces the single header line and leaves every other byte identical.
    3. Grep-verify entity-ID stability and placeholder-safety on the **target** file:
       - `grep -c "luxtronik2_modbus_proxy" <target>` MUST return `0`
       - `grep -c "luxtronik_2_0_" <target>` MUST return `32` (must equal `grep -c luxtronik_2_0_` on the source — proves nothing was lost)
       - `grep -c "192\.168\." <target>` MUST return `0` (IP sanitized)
       - `grep -c "wolbeck" <target>` MUST return `0` (hostname sanitized — catches both the FQDN and any stray reference)
       - `grep -q "your-heatpump-ip" <target>` MUST succeed (placeholder present)
    4. **Do NOT** run a `diff -q` byte-identity check — the files are intentionally non-identical on line 11. Byte identity is the wrong invariant; entity-ID preservation + sanitization is the right one.
    5. Cross-check line count: `wc -l` on source and target must match (sanitization replaces one line in place, does not add/remove lines).
    6. **D-04(c) link check:** Verify the dashboard is referenced from the new repo's discoverable docs:
       `grep -l 'dashboard-waermepumpe' /home/dwolbeck/claude-code/luxtronik2-hass/README.md /home/dwolbeck/claude-code/luxtronik2-hass/MIGRATION.md 2>/dev/null`
       If neither file references the dashboard, this is NOT a blocker for Phase 12 (D-03 — bugs/gaps → follow-up), but the gap MUST be recorded in `12-VERIFICATION.md` Pre-flight Check table (Task 3) as a follow-up item.
    7. Do NOT commit in the new repo — leave the file unstaged. The maintainer decides when to push. Record the path, grep counts, and D-04(c) link check outcome for Plan 02's VERIFICATION.md pre-flight section.

    DO NOT modify any other file in the new repo. DO NOT touch `custom_components/`, `README*`, `MIGRATION.md`, `hacs.json`, `pyproject.toml`, or `tests/`. This task is additive only and writes exactly one file.
  </action>
  <verify>
    <automated>TARGET=/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml; SOURCE=/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/docs/examples/dashboard-waermepumpe.yaml; test -f "$TARGET" && [ "$(grep -c 'luxtronik2_modbus_proxy' "$TARGET")" = "0" ] && [ "$(grep -c 'luxtronik_2_0_' "$TARGET")" = "$(grep -c 'luxtronik_2_0_' "$SOURCE")" ] && [ "$(grep -c '192\.168\.' "$TARGET")" = "0" ] && [ "$(grep -c 'wolbeck' "$TARGET")" = "0" ] && grep -q 'your-heatpump-ip' "$TARGET" && [ "$(wc -l < "$TARGET")" = "$(wc -l < "$SOURCE")" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml`
    - `grep -c luxtronik2_modbus_proxy` returns exactly `0`
    - `grep -c luxtronik_2_0_` on target equals `grep -c luxtronik_2_0_` on source (entity-ID count preserved)
    - `grep -c '192\.168\.'` returns exactly `0` (no IPs — sanitization succeeded)
    - `grep -c 'wolbeck'` returns exactly `0` (no hostname — sanitization succeeded)
    - `grep -q 'your-heatpump-ip'` succeeds (placeholder present on line 11)
    - `wc -l` matches between source and target (one-line in-place replacement, not additive)
    - D-04(c) link-check outcome is recorded for Task 3's VERIFICATION.md pre-flight table (PASS if dashboard is referenced from README.md or MIGRATION.md in the new repo, else recorded as follow-up gap)
    - No other files in the new repo are modified (`cd /home/dwolbeck/claude-code/luxtronik2-hass && git status --porcelain | grep -v '^?? docs/examples/dashboard-waermepumpe.yaml$'` MUST return empty)
  </acceptance_criteria>
  <done>
    Dashboard mirrored into new repo, entity-ID stability verified, no other files touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create maintainer migration checklist (12-HUMAN-UAT.md)</name>
  <files>.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/12-maintainer-migration-verification/12-CONTEXT.md (D-06 — exact 7-step sequence)
    - .planning/ROADMAP.md §"Phase 12" (success criteria language)
    - .planning/REQUIREMENTS.md §"Maintainer Migration (MIGRATE)"
  </read_first>
  <action>
    Create `.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md` with the exact content below. The checklist must contain exactly 7 numbered steps corresponding to D-06. No automation hints, no API calls, no backup/rollback (D-01, D-02). Placeholder `your-heatpump-ip` only — no real IPs (CLAUDE.md security rule).

    Exact file content:

    ```markdown
    # Phase 12: Maintainer Migration — Human UAT Checklist

    **Phase:** 12-maintainer-migration-verification
    **Target instance:** Maintainer's own HA 2026.4.1 (Supervised)
    **Execution model:** Manual — no automation, no backup, no rollback (per D-01, D-02)
    **Acceptance signal:** Dashboard `docs/examples/dashboard-waermepumpe.yaml` renders all 32 values with zero "entity not found" errors (D-05)

    ---

    ## Before You Start

    - Expected outcome: 31 entities under the stable `luxtronik_2_0_*` device slug (unchanged from v1.1), 1 dashboard rendering cleanly, zero automations/scripts/templates affected.
    - Maintainer explicitly opted out of backup and rollback. If anything breaks, the fallback is to re-add the archived `notDIRK/luxtronik2-modbus-proxy` HACS custom repository and reinstall the old version — not a scripted rollback.
    - Have the heat pump IP ready (referred to below as `your-heatpump-ip`).

    ## Checklist

    1. **Delete the old config entry.** In HA UI: Settings → Devices & Services → find `Luxtronik 2.0` (domain `luxtronik2_modbus_proxy`) → ⋮ → Delete. Confirm no entities remain under that domain (Settings → Devices & Services → Entities → filter by `luxtronik2_modbus_proxy` returns zero rows).

    2. **Remove the old HACS custom repository.** HACS → ⋮ → Custom repositories → find `notDIRK/luxtronik2-modbus-proxy` → Remove. Then uninstall the old integration from HACS if it still appears in the installed list.

    3. **Add the new HACS custom repository.** HACS → ⋮ → Custom repositories → Repository: `https://github.com/notDIRK/luxtronik2-hass` → Category: Integration → Add. The dialog must accept the URL without manifest/hacs.json errors.

    4. **Install the new integration.** HACS → Integrations → search `Luxtronik 2.0` (new entry from `luxtronik2-hass`) → Download → select the latest release.

    5. **Restart Home Assistant.** Settings → System → Restart → Restart Home Assistant. Wait for HA to come back up.

    6. **Run the config flow.** Settings → Devices & Services → Add Integration → search `Luxtronik 2.0` → enter `your-heatpump-ip` (the maintainer's actual IP — DO NOT paste it into any Phase 12 artifact afterwards). The config flow must complete without connection errors.

    7. **Open the dashboard and visually confirm all tiles render.** Lovelace → navigate to the Wärmepumpe dashboard → verify every tile shows a value (no "entity not found" red errors, no `unknown`/`unavailable` placeholders on entities that previously worked). Reference file: `docs/examples/dashboard-waermepumpe.yaml` in the `luxtronik2-hass` repo (32 entity references, all `luxtronik_2_0_*`).

    ## After You're Done

    Return to this repo and populate `12-VERIFICATION.md`:
    - Pre-flight Check section was pre-filled by Plan 01 (dashboard mirrored + grep verified).
    - Fill in the **Maintainer Action Log** section — one line per step, with timestamp and outcome (OK / issue description).
    - Fill in the **Post-Migration Record** — HA version, date, entity count (expected: 31), dashboard render result (all 32 OK, or list of failing entities), any follow-up bugs to file.
    - Then run Plan 02's second task to append the "Maintainer verified" note to `MIGRATION.md` in the new repo.

    ## Security Reminder

    Do NOT paste real IPs, hostnames, or credentials into `12-VERIFICATION.md`, `MIGRATION.md`, or any artifact that could be committed. Use `your-heatpump-ip` as a placeholder. This is a PUBLIC repo (see CLAUDE.md).
    ```

    Write the file with that exact content (markdown code fence shown above is for visual clarity — the actual file is the content inside it without the outer ```markdown / ``` fences).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md && [ "$(grep -c '^[1-7]\. ' .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md)" = "7" ] && grep -q '## Checklist' .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md && grep -q 'your-heatpump-ip' .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md && ! grep -qE '192\.168\.[0-9]+\.[0-9]+' .planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md`
    - `grep -c '^[1-7]\. '` returns exactly `7` (seven top-level numbered steps, matching D-06)
    - Contains literal strings `## Checklist`, `Delete the old config entry`, `Remove the old HACS custom repository`, `Add the new HACS custom repository`, `Install the new integration`, `Restart Home Assistant`, `config flow`, `dashboard`
    - Contains placeholder `your-heatpump-ip` and does NOT contain any real IP matching `192.168.*` or real hostname ending in `.local` / `.net`
    - Contains no mention of "backup", "rollback", "REST API", "WebSocket" as migration actions (D-01, D-02 — rollback is mentioned only as a disclaimer in "Before You Start")
  </acceptance_criteria>
  <done>
    Copy-pasteable 7-step checklist exists, placeholder-safe, aligned with D-06 wording.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 12-VERIFICATION.md audit-trail skeleton</name>
  <files>.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-publish-archive-legacy/11-VERIFICATION.md (format template — section headings, date/phase/status frontmatter, roadmap-text quoting pattern)
    - .planning/ROADMAP.md §"Phase 12" (3 success criteria verbatim)
    - .planning/REQUIREMENTS.md §"Maintainer Migration (MIGRATE)" (MIGRATE-01/02/03 acceptance text)
    - .planning/phases/12-maintainer-migration-verification/12-CONTEXT.md (D-08: what goes in this file)
  </read_first>
  <action>
    Create `.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md` as a **skeleton** — Plan 01 pre-fills the Pre-flight Check section (which Claude can verify autonomously); the Maintainer Action Log and Post-Migration Record sections are stubbed with `TBD — filled in Plan 02 after maintainer runs checklist`.

    Exact file content (between the ===BEGIN===/===END=== markers — do not include the markers themselves):

    ===BEGIN===
    # Phase 12: Maintainer Migration Verification — Verification

    **Date:** 2026-04-10 (pre-flight) / TBD (maintainer execution)
    **Phase:** 12-maintainer-migration-verification
    **Plans:** 12-01-PLAN.md (pre-flight + artifacts), 12-02-PLAN.md (maintainer execution + recording)
    **Mode:** execute — Plan 01 autonomous, Plan 02 requires maintainer action
    **Status:** IN PROGRESS — pre-flight complete, awaiting maintainer execution

    ---

    ## Requirements Covered

    - **MIGRATE-01** — Old `luxtronik2_modbus_proxy` config entry deleted, no ghost entities
    - **MIGRATE-02** — New `luxtronik2_hass` integration installed via HACS custom repository, config flow succeeds
    - **MIGRATE-03** — All 31 entities re-appear under `luxtronik_2_0_*`; dashboard renders all 32 referenced values

    ---

    ## Pre-flight Check

    *(Completed autonomously in Plan 01, 2026-04-10.)*

    ### 1. Dashboard mirrored into new repo (D-07)

    **Source:** `/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/docs/examples/dashboard-waermepumpe.yaml`
    **Target:** `/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml`

    | Check | Command | Expected | Result |
    |-------|---------|----------|--------|
    | File exists in new repo | `test -f <target>` | exit 0 | PASS |
    | No legacy domain refs | `grep -c luxtronik2_modbus_proxy <target>` | `0` | PASS |
    | Stable device slug refs (count matches source) | `grep -c luxtronik_2_0_ <target>` vs `<source>` | equal | PASS |
    | IP sanitized (line 11) | `grep -c '192\.168\.' <target>` | `0` | PASS |
    | Hostname sanitized (line 11) | `grep -c 'wolbeck' <target>` | `0` | PASS |
    | Placeholder present | `grep -q 'your-heatpump-ip' <target>` | exit 0 | PASS |
    | Line count preserved (in-place replacement) | `wc -l <target>` vs `<source>` | equal | PASS |
    | D-04(c) dashboard referenced from new repo docs | `grep -l 'dashboard-waermepumpe' <new-repo>/README.md <new-repo>/MIGRATION.md` | at least one match | **PASS / FOLLOW-UP** (Task 1 records actual outcome; if no match, log as follow-up per D-03 — not a Phase 12 blocker) |
    | No other new-repo files modified | `cd <new-repo> && git status --porcelain \| grep -v '^?? docs/examples/dashboard-waermepumpe.yaml$'` | empty | PASS |

    ### 2. Checklist exists (D-06)

    **File:** `.planning/phases/12-maintainer-migration-verification/12-HUMAN-UAT.md`

    | Check | Command | Expected | Result |
    |-------|---------|----------|--------|
    | File exists | `test -f 12-HUMAN-UAT.md` | exit 0 | PASS |
    | Seven numbered steps | `grep -c '^[1-7]\. ' 12-HUMAN-UAT.md` | `7` | PASS |
    | Placeholder-safe (no real IPs) | `grep -E '192\.168\.[0-9]+\.[0-9]+' 12-HUMAN-UAT.md` | empty | PASS |

    **Pre-flight status:** GREEN — ready for maintainer execution.

    ---

    ## Maintainer Action Log

    *(TBD — filled in Plan 02 after maintainer runs `12-HUMAN-UAT.md` checklist. One line per step with timestamp and outcome.)*

    | Step | Action | Timestamp | Outcome |
    |------|--------|-----------|---------|
    | 1 | Delete old config entry | TBD | TBD |
    | 2 | Remove old HACS custom repository | TBD | TBD |
    | 3 | Add new HACS custom repository (`notDIRK/luxtronik2-hass`) | TBD | TBD |
    | 4 | Install new integration | TBD | TBD |
    | 5 | Restart HA | TBD | TBD |
    | 6 | Run config flow with heat pump IP | TBD | TBD |
    | 7 | Open dashboard and visually confirm tiles | TBD | TBD |

    ---

    ## Post-Migration Record

    *(TBD — filled in Plan 02.)*

    - **HA version:** TBD (expected: 2026.4.1)
    - **Date of maintainer execution:** TBD
    - **Old domain ghost entities:** TBD (expected: 0) → MIGRATE-01 acceptance
    - **New integration config flow:** TBD (expected: completed without errors) → MIGRATE-02 acceptance
    - **Entity count under `luxtronik_2_0_*` post-migration:** TBD (expected: 31) → MIGRATE-03 entity acceptance
    - **Dashboard render:** TBD (expected: all 32 values render, zero "entity not found" errors) → MIGRATE-03 dashboard acceptance (D-05)
    - **Bugs/issues surfaced:** TBD (filed as follow-ups per D-03, NOT fixed inside Phase 12)

    ---

    ## Success Criteria (from ROADMAP.md Phase 12)

    1. **MIGRATE-01:** Old config entry deleted, entity registry shows zero `luxtronik2_modbus_proxy` entities → **TBD**
    2. **MIGRATE-02:** New `luxtronik2_hass` installed via HACS custom repository `notDIRK/luxtronik2-hass`, config flow completes with heat pump IP → **TBD**
    3. **MIGRATE-03:** 31 entities under `luxtronik_2_0_*` slug, dashboard renders all 32 values without errors → **TBD**

    **Overall status:** IN PROGRESS — will flip to PASSED/FAILED in Plan 02.
    ===END===

    Write the file with exactly the content between the markers (do NOT include the `===BEGIN===` / `===END===` lines themselves).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q '## Pre-flight Check' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q '## Maintainer Action Log' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q '## Post-Migration Record' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-01' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-02' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && grep -q 'MIGRATE-03' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md && ! grep -qE '192\.168\.[0-9]+\.[0-9]+' .planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/12-maintainer-migration-verification/12-VERIFICATION.md`
    - Contains all three section headings: `## Pre-flight Check`, `## Maintainer Action Log`, `## Post-Migration Record`
    - References all three requirement IDs: `MIGRATE-01`, `MIGRATE-02`, `MIGRATE-03`
    - Pre-flight Check section is marked PASS (all 5 pre-flight checks recorded as completed)
    - Maintainer Action Log and Post-Migration Record sections contain `TBD` placeholders
    - Status line shows `IN PROGRESS`
    - No real IPs matching `192.168.*` anywhere in the file
  </acceptance_criteria>
  <done>
    VERIFICATION.md skeleton exists with pre-flight completed and TBD placeholders ready for Plan 02 to populate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Doc-only phase — no code changes, no network listeners, no user inputs, no secrets handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Phase 12 markdown artifacts (VERIFICATION.md, HUMAN-UAT.md) | mitigate | PUBLIC repo — acceptance criteria explicitly grep-forbid `192.168.*` patterns and require `your-heatpump-ip` placeholder (CLAUDE.md rule). Pre-commit hook blocks on leak. |
| T-12-02 | Information Disclosure | Dashboard copy into new PUBLIC repo (source has real IP+hostname on line 11) | mitigate | The target `luxtronik2-hass` is a NEW public repo where this file does NOT yet exist — copying unmodified would be a first-time publication of real network data. Task 1 sanitizes line 11 during copy via `sed` (IP+hostname → `your-heatpump-ip`); verify step grep-asserts `192.168.*`=0 and `wolbeck`=0 on the target. The legacy-repo copy retaining the original header is a separate pre-existing issue tracked outside Phase 12 per D-03. |
</threat_model>

<verification>
- Plan 01 is fully autonomous: all three tasks verify via local filesystem commands (test, grep, diff, git status).
- No HA API calls, no network activity, no maintainer interaction required for Plan 01.
- Pre-flight state must be GREEN before Plan 02 runs.
</verification>

<success_criteria>
- Dashboard exists at `/home/dwolbeck/claude-code/luxtronik2-hass/docs/examples/dashboard-waermepumpe.yaml` with line 11 sanitized (`your-heatpump-ip` placeholder) and all entity IDs preserved (`grep -c luxtronik_2_0_` matches source count).
- `12-HUMAN-UAT.md` contains exactly 7 numbered checklist steps matching D-06, no real IPs.
- `12-VERIFICATION.md` contains Pre-flight Check (marked PASS), Maintainer Action Log (TBD), Post-Migration Record (TBD) sections.
- New repo working tree shows only the one new file (`docs/examples/dashboard-waermepumpe.yaml` as untracked), no other modifications.
- MIGRATE-03 pre-flight prerequisites (dashboard + entity-ID stability) are verified; MIGRATE-01 and MIGRATE-02 acceptance is deferred to Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/12-maintainer-migration-verification/12-01-SUMMARY.md`.
</output>
