---
phase: 08-new-repo-extraction-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/dwolbeck/claude-code/luxtronik2-hass/.git/
  - /home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml
  - /home/dwolbeck/claude-code/luxtronik2-hass/README.md
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_sensor.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_map.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_definitions.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_cache.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_config.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_sg_ready.py
  - /home/dwolbeck/claude-code/luxtronik2-hass/tests/integration/test_proxy_mock.py
autonomous: true
requirements:
  - SPLIT-02
  - SPLIT-03

must_haves:
  truths:
    - "A fresh working copy exists at /home/dwolbeck/claude-code/luxtronik2-hass with no remote origin configured"
    - "git log -- custom_components/luxtronik2_modbus_proxy/coordinator.py shows Phase 4-7 commits (matched by message, author, date; SHAs are rewritten by filter-repo and that is expected)"
    - "git blame resolves on coordinator.py and config_flow.py to original commit messages and authors"
    - "The working copy contains no src/, no Dockerfile, no docker-compose.yml, no config.example.yaml, no mkdocs.yml, no docs/, no contrib/"
    - "A trimmed pyproject.toml (dev deps only, no proxy entry points, package name unchanged) and a placeholder README.md exist"
    - "pytest passes on the surviving tests (test_select.py + test_number.py)"
  artifacts:
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/.git"
      provides: "Rewritten git history preserving HACS integration commits"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_modbus_proxy/coordinator.py"
      provides: "HACS integration coordinator with preserved git blame"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/custom_components/luxtronik2_modbus_proxy/config_flow.py"
      provides: "HACS integration config flow with preserved git blame"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/hacs.json"
      provides: "HACS validation marker (required at repo root)"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml"
      provides: "Trimmed dev-only pyproject (no proxy entry points)"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/README.md"
      provides: "Placeholder README (rewritten in Phase 10)"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_select.py"
      provides: "Surviving HA select test (ast-based, no proxy imports)"
    - path: "/home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_number.py"
      provides: "Surviving HA number test (ast-based, no proxy imports)"
  key_links:
    - from: "git clone --no-local source"
      to: "/home/dwolbeck/claude-code/luxtronik2-hass"
      via: "fresh clone of PUBLIC-luxtronik2-modbus-proxy"
      pattern: "clone --no-local"
    - from: "git filter-repo pass 1"
      to: "path whitelist"
      via: "custom_components/ tests/ .github/ hacs.json pyproject.toml README.md README.de.md"
      pattern: "filter-repo --path"
    - from: "git filter-repo pass 2"
      to: "invert-paths drop"
      via: ".github/workflows/publish.yml + docs.yml"
      pattern: "filter-repo --force --invert-paths"
---

<objective>
Create a clean local working copy of the future `luxtronik2-hass` repo at `/home/dwolbeck/claude-code/luxtronik2-hass` by cloning the current repo and running `git filter-repo` with a path whitelist. Verify history is preserved. Prune proxy-coupled tests. Trim `pyproject.toml` and write a placeholder `README.md`. Run pytest to prove the surviving tree is coherent.

Purpose: Phase 8 is the first step of the v1.2 repo split. It is strictly local (no push, no GitHub, no remote). All later phases (9 rename, 10 docs, 11 publish, 12 migrate) depend on this clean extraction existing.

Output: A working directory at `/home/dwolbeck/claude-code/luxtronik2-hass` with rewritten git history, no proxy artifacts, and passing pytest. No modifications to the source repo except this plan's SUMMARY file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/CLAUDE.md
@/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/STATE.md
@/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/ROADMAP.md
@/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/REQUIREMENTS.md
@/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-RESEARCH.md

<interfaces>
<!-- Critical pre-flight facts — executor must not discover by exploration -->

Source repo (read-only for this phase):
  /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy

Target working copy (created fresh; must NOT pre-exist):
  /home/dwolbeck/claude-code/luxtronik2-hass

Tools required (verify before destructive ops):
  - git (2.34.1, system)
  - git-filter-repo (installed at /home/dwolbeck/.local/bin/git-filter-repo)
  - python3 (3.10.12, system)
  - pytest (>=9.0; install via `pip install --user pytest pytest-asyncio` if missing)

Filter-repo two-pass command (EXACT):

  Pass 1 (whitelist; run in fresh clone):
    git filter-repo \
      --path custom_components/ \
      --path tests/ \
      --path .github/ \
      --path hacs.json \
      --path pyproject.toml \
      --path README.md \
      --path README.de.md

  Pass 2 (drop proxy-specific workflows; --force required):
    git filter-repo --force --invert-paths \
      --path .github/workflows/publish.yml \
      --path .github/workflows/docs.yml

SHA rewrite interpretation (locked by orchestrator):
  SHAs are rewritten by filter-repo — this is EXPECTED and ACCEPTABLE.
  SPLIT-02 is verified by comparing commit MESSAGES + AUTHORS + DATES, not byte-identical SHA strings.

Tests to DELETE in the new working copy (7 files — they import from src/ proxy package which no longer exists):
  tests/unit/test_sensor.py
  tests/unit/test_register_map.py
  tests/unit/test_register_definitions.py
  tests/unit/test_register_cache.py
  tests/unit/test_config.py
  tests/unit/test_sg_ready.py
  tests/integration/test_proxy_mock.py

Tests to KEEP (pure-ast parsing, no HA or proxy imports):
  tests/unit/test_select.py
  tests/unit/test_number.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks + fresh clone + filter-repo extraction</name>
  <files>/home/dwolbeck/claude-code/luxtronik2-hass/.git/</files>
  <read_first>
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-RESEARCH.md (sections: "The Exact filter-repo Command", "Common Pitfalls" 1/2/3/7)
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/CLAUDE.md (security: no IPs/hostnames/credentials in any output)
  </read_first>
  <action>
    Execute the following commands in order. DO NOT substitute paths. DO NOT push. DO NOT add a remote.

    Step 1 — Pre-flight (fail fast):
      command -v git-filter-repo || { echo "FAIL: git-filter-repo not in PATH"; exit 1; }
      git-filter-repo --version
      test ! -e /home/dwolbeck/claude-code/luxtronik2-hass || { echo "FAIL: target already exists"; exit 1; }

    Step 2 — Fresh clone with --no-local (forces real object copy, not hardlinks):
      cd /home/dwolbeck/claude-code
      git clone --no-local PUBLIC-luxtronik2-modbus-proxy luxtronik2-hass
      cd luxtronik2-hass

    Step 3 — Drop remote origin immediately (defensive; Phase 8 is local-only):
      git remote remove origin

    Step 4 — Filter-repo PASS 1 (whitelist of paths to keep). Run from inside /home/dwolbeck/claude-code/luxtronik2-hass:
      git filter-repo \
        --path custom_components/ \
        --path tests/ \
        --path .github/ \
        --path hacs.json \
        --path pyproject.toml \
        --path README.md \
        --path README.de.md

    Step 5 — Filter-repo PASS 2 (drop proxy-specific workflows; --force required because repo is no longer "fresh"):
      git filter-repo --force --invert-paths \
        --path .github/workflows/publish.yml \
        --path .github/workflows/docs.yml

    Step 6 — Confirm no origin was restored and no stale __pycache__ exists:
      git remote -v
      find tests -name __pycache__ -exec rm -rf {} + 2>/dev/null || true

    If any step fails, stop. Do NOT attempt to "fix" the partial state — delete /home/dwolbeck/claude-code/luxtronik2-hass and restart the task.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && test -d .git && test -d custom_components/luxtronik2_modbus_proxy && test -f hacs.json && [ -z "$(git remote)" ] && test ! -e .github/workflows/publish.yml && test ! -e .github/workflows/docs.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/home/dwolbeck/claude-code/luxtronik2-hass/.git` exists
    - `git remote` output is empty (zero lines) inside the new working copy
    - `custom_components/luxtronik2_modbus_proxy/` directory exists in the new working copy
    - `hacs.json` exists at the root of the new working copy
    - `pyproject.toml` exists at the root of the new working copy (from history, not yet trimmed)
    - `.github/workflows/publish.yml` does NOT exist in the new working copy
    - `.github/workflows/docs.yml` does NOT exist in the new working copy
    - `.git/filter-repo/commit-map` exists (auto-generated by filter-repo)
    - No `__pycache__` directories exist under `tests/`
  </acceptance_criteria>
  <done>Fresh clone at /home/dwolbeck/claude-code/luxtronik2-hass has been extracted via two-pass filter-repo, has no remote, and contains only the whitelisted paths minus proxy-specific workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Verify history preservation and forbidden-file absence</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-RESEARCH.md (sections: "Pitfall 2", "Verification Recipes", "Phase Requirements -> Test Map")
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/ROADMAP.md (Phase 8 Success Criteria 2 and 3)
  </read_first>
  <action>
    Run these verification commands inside `/home/dwolbeck/claude-code/luxtronik2-hass`. They MUST all pass. Record their output for the SUMMARY.

    SC-2 (history preservation):
      cd /home/dwolbeck/claude-code/luxtronik2-hass
      COORD_COUNT=$(git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py | wc -l)
      CONFIG_COUNT=$(git log --oneline -- custom_components/luxtronik2_modbus_proxy/config_flow.py | wc -l)
      test "$COORD_COUNT" -ge 2 || { echo "FAIL: coordinator.py has $COORD_COUNT commits (expected >=2)"; exit 1; }
      test "$CONFIG_COUNT" -ge 2 || { echo "FAIL: config_flow.py has $CONFIG_COUNT commits (expected >=2)"; exit 1; }

    SC-2 (blame resolves — lines must have real SHAs, not zeros):
      git blame custom_components/luxtronik2_modbus_proxy/coordinator.py | head -5
      git blame custom_components/luxtronik2_modbus_proxy/config_flow.py | head -5
      # Assert no lines start with 00000000 (uncommitted):
      git blame custom_components/luxtronik2_modbus_proxy/coordinator.py | head -5 | awk '{print $1}' | grep -v '^0\{8\}' > /dev/null || { echo "FAIL: coordinator.py blame has uncommitted lines"; exit 1; }

    SC-2 (commit messages match Phase 4-7 originals — spot-check by searching for known phase prefixes):
      git log --oneline -- custom_components/luxtronik2_modbus_proxy/ | grep -E '\((04|05|06|07)' | head -5
      # Expect at least one match for phase-numbered commits

    SC-3 (forbidden files absent — use `find` and `test`):
      for forbidden in src Dockerfile docker-compose.yml config.example.yaml mkdocs.yml contrib docs; do
        test ! -e "$forbidden" || { echo "FAIL: $forbidden exists"; exit 1; }
      done
      find . -maxdepth 2 -name 'luxtronik2-modbus-proxy.service' -print | grep -q . && { echo "FAIL: systemd unit found"; exit 1; } || true

    Listing for SUMMARY.md:
      find . -type f -not -path './.git/*' | sort > /tmp/phase8-tree.txt
      wc -l /tmp/phase8-tree.txt
      head -50 /tmp/phase8-tree.txt
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && test $(git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py | wc -l) -ge 2 && test $(git log --oneline -- custom_components/luxtronik2_modbus_proxy/config_flow.py | wc -l) -ge 2 && test ! -e src && test ! -e Dockerfile && test ! -e docker-compose.yml && test ! -e config.example.yaml && test ! -e mkdocs.yml && test ! -e contrib && test ! -e docs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py | wc -l` >= 2
    - `git log --oneline -- custom_components/luxtronik2_modbus_proxy/config_flow.py | wc -l` >= 2
    - `git blame custom_components/luxtronik2_modbus_proxy/coordinator.py | head -5` produces non-zero SHAs (not `00000000`)
    - `git log --oneline -- custom_components/luxtronik2_modbus_proxy/` contains at least one commit message with a phase prefix `(04`, `(05`, `(06`, or `(07`
    - None of these paths exist at the repo root: `src`, `Dockerfile`, `docker-compose.yml`, `config.example.yaml`, `mkdocs.yml`, `contrib`, `docs`
    - No file matching `luxtronik2-modbus-proxy.service` exists anywhere in the working copy
  </acceptance_criteria>
  <done>History preservation verified (SPLIT-02) and forbidden files confirmed absent (SPLIT-03). Output captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Trim pyproject.toml, write placeholder README.md, delete proxy-coupled tests</name>
  <files>
    /home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml
    /home/dwolbeck/claude-code/luxtronik2-hass/README.md
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_sensor.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_map.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_definitions.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_register_cache.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_config.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/unit/test_sg_ready.py
    /home/dwolbeck/claude-code/luxtronik2-hass/tests/integration/test_proxy_mock.py
  </files>
  <read_first>
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-RESEARCH.md (sections: "Trimmed pyproject.toml", "Placeholder README.md", "Test Pruning Strategy")
    - /home/dwolbeck/claude-code/luxtronik2-hass/pyproject.toml (current content after filter-repo)
  </read_first>
  <action>
    Work inside `/home/dwolbeck/claude-code/luxtronik2-hass`.

    Step 1 — Overwrite `pyproject.toml` with the EXACT trimmed content below. Note: package name stays `luxtronik2-modbus-proxy` (Phase 9 renames it). NO proxy entry points. NO `src/` pythonpath. NO runtime deps (HA installs them via manifest.json).

      [project]
      name = "luxtronik2-modbus-proxy"
      version = "1.1.2"
      description = "Home Assistant custom integration for Luxtronik 2.0 heat pumps"
      requires-python = ">=3.10"
      license = { text = "MIT" }
      readme = "README.md"
      keywords = ["luxtronik", "heat-pump", "home-assistant", "hacs"]
      classifiers = [
          "Development Status :: 4 - Beta",
          "Intended Audience :: End Users/Desktop",
          "License :: OSI Approved :: MIT License",
          "Programming Language :: Python :: 3",
          "Programming Language :: Python :: 3.10",
          "Programming Language :: Python :: 3.11",
          "Programming Language :: Python :: 3.12",
          "Topic :: Home Automation",
      ]

      [project.optional-dependencies]
      dev = [
          "pytest>=9.0",
          "pytest-asyncio>=1.3",
          "pytest-cov",
          "ruff",
          "mypy",
      ]

      [tool.pytest.ini_options]
      asyncio_mode = "auto"
      testpaths = ["tests"]

      [tool.ruff]
      target-version = "py310"
      line-length = 100

      [tool.ruff.lint]
      select = ["E", "F", "W", "I", "D", "UP", "B", "SIM", "RUF"]

      [tool.ruff.lint.pydocstyle]
      convention = "google"

      [tool.mypy]
      strict = true
      python_version = "3.10"

    Step 2 — Overwrite `README.md` with this EXACT placeholder (no real IPs, hostnames, or credentials — CLAUDE.md security rule):

      # luxtronik2-modbus-proxy (HACS integration extract)

      Local working copy created via `git filter-repo` from
      `PUBLIC-luxtronik2-modbus-proxy` on 2026-04-10 as part of milestone v1.2
      Phase 8 (Repo Split & HA-First Rebrand).

      This is an **intermediate working state**:

      - The internal name is still `luxtronik2_modbus_proxy` — Phase 9 renames it to `luxtronik2_hass`.
      - This README is a placeholder — Phase 10 rewrites it with three-path positioning (Supported / Experimental / Planned).
      - There is no remote origin — Phase 11 pushes to `notDIRK/luxtronik2-hass`.

      See `.planning/ROADMAP.md` in the source repo for context.

    Step 3 — Delete the 7 proxy-coupled tests (they import from the old `src/` proxy package which was filtered out):
      cd /home/dwolbeck/claude-code/luxtronik2-hass
      rm -f tests/unit/test_sensor.py
      rm -f tests/unit/test_register_map.py
      rm -f tests/unit/test_register_definitions.py
      rm -f tests/unit/test_register_cache.py
      rm -f tests/unit/test_config.py
      rm -f tests/unit/test_sg_ready.py
      rm -f tests/integration/test_proxy_mock.py
      # Remove empty integration dir if it becomes empty:
      rmdir tests/integration 2>/dev/null || true

    Step 4 — Commit all changes as a single "chore" commit (the pre-commit hook is not installed in the new clone — filter-repo only copies tracked files, not `.git/hooks/`. This is expected; secret scan runs in Phase 11 before push):
      git add -A
      git status
      git commit -m "chore(08): trim pyproject, add placeholder README, drop proxy-coupled tests

Phase 8 extraction cleanup:
- Trim pyproject.toml to dev-only (no proxy entry points, no src/ layout)
- Replace README.md with placeholder (Phase 10 will rewrite)
- Delete 7 tests that import from the removed src/ proxy package;
  test_select.py and test_number.py survive (pure ast parsing)"
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && ! grep -q 'luxtronik2_modbus_proxy.main:cli' pyproject.toml && ! grep -q '\[project.scripts\]' pyproject.toml && grep -q 'name = "luxtronik2-modbus-proxy"' pyproject.toml && test -f README.md && grep -q 'HACS integration extract' README.md && test ! -f tests/unit/test_sensor.py && test ! -f tests/unit/test_register_map.py && test ! -f tests/unit/test_register_definitions.py && test ! -f tests/unit/test_register_cache.py && test ! -f tests/unit/test_config.py && test ! -f tests/unit/test_sg_ready.py && test ! -f tests/integration/test_proxy_mock.py && test -f tests/unit/test_select.py && test -f tests/unit/test_number.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'luxtronik2_modbus_proxy.main:cli' pyproject.toml` returns non-zero (entry point removed)
    - `grep -q '\[project.scripts\]' pyproject.toml` returns non-zero (no scripts section)
    - `grep -q 'name = "luxtronik2-modbus-proxy"' pyproject.toml` returns zero (name unchanged, per Phase 8 no-rename rule)
    - `grep -q 'pytest>=9.0' pyproject.toml` returns zero
    - `README.md` exists and contains the string "HACS integration extract"
    - `README.md` contains zero real IP addresses (only `192.168.x.x`-style placeholders if any), zero hostnames, zero credentials (CLAUDE.md rule)
    - All 7 proxy-coupled test files are absent: `tests/unit/test_sensor.py`, `test_register_map.py`, `test_register_definitions.py`, `test_register_cache.py`, `test_config.py`, `test_sg_ready.py`, `tests/integration/test_proxy_mock.py`
    - `tests/unit/test_select.py` still exists
    - `tests/unit/test_number.py` still exists
    - `git status` is clean (changes committed)
    - `git log -1 --oneline` shows the "chore(08):" commit
  </acceptance_criteria>
  <done>pyproject.toml trimmed, placeholder README.md written, 7 proxy-coupled tests deleted, all changes committed as a single chore commit.</done>
</task>

<task type="auto">
  <name>Task 4: Run pytest + final phase verification</name>
  <files>(verification only)</files>
  <read_first>
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-RESEARCH.md (section: "Verification script")
    - /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/ROADMAP.md (Phase 8 Success Criteria 1-4)
  </read_first>
  <action>
    Run the full Phase 8 verification script inside `/home/dwolbeck/claude-code/luxtronik2-hass`. All SC checks must pass.

    Step 1 — Ensure pytest is available. If missing, install it for the current user (no sudo):
      python3 -m pytest --version || python3 -m pip install --user 'pytest>=9.0' 'pytest-asyncio>=1.3'

    Step 2 — Run pytest on the surviving tests. They are pure-ast parsing, no HA runtime required.
      cd /home/dwolbeck/claude-code/luxtronik2-hass
      python3 -m pytest tests/unit/test_select.py tests/unit/test_number.py -x -q

    Step 3 — Run the full phase verification script:
      cd /home/dwolbeck/claude-code/luxtronik2-hass
      fail=0

      # SC-1: no remote origin
      if git remote | grep -q .; then echo "FAIL SC-1: remote set"; fail=1; else echo "PASS SC-1"; fi

      # SC-2: history preserved
      for f in custom_components/luxtronik2_modbus_proxy/coordinator.py custom_components/luxtronik2_modbus_proxy/config_flow.py; do
        count=$(git log --oneline -- "$f" | wc -l)
        if [ "$count" -lt 2 ]; then echo "FAIL SC-2: $f has $count commits"; fail=1; else echo "PASS SC-2: $f has $count commits"; fi
      done

      # SC-3: forbidden absent
      for x in src Dockerfile docker-compose.yml config.example.yaml mkdocs.yml contrib docs; do
        if [ -e "$x" ]; then echo "FAIL SC-3: $x exists"; fail=1; fi
      done
      [ ! -e src ] && echo "PASS SC-3"

      # SC-4: trimmed pyproject, placeholder README, pytest green
      if grep -q 'luxtronik2_modbus_proxy.main:cli' pyproject.toml; then echo "FAIL SC-4: proxy entry point still in pyproject"; fail=1; else echo "PASS SC-4 pyproject"; fi
      if [ ! -f README.md ]; then echo "FAIL SC-4: no README"; fail=1; else echo "PASS SC-4 README"; fi
      python3 -m pytest tests/unit/test_select.py tests/unit/test_number.py -x -q && echo "PASS SC-4 pytest" || { echo "FAIL SC-4 pytest"; fail=1; }

      exit $fail

    Step 4 — Generate tree listing for the SUMMARY (first 60 lines):
      find . -type f -not -path './.git/*' | sort | head -60

    Step 5 — Record in SUMMARY: tree listing, commit count for coordinator.py and config_flow.py, pytest output, size of `.git/filter-repo/commit-map`. The SUMMARY file goes in the SOURCE repo at `.planning/phases/08-new-repo-extraction-setup/08-01-SUMMARY.md` per the `<output>` block below.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/luxtronik2-hass && python3 -m pytest tests/unit/test_select.py tests/unit/test_number.py -x -q && [ -z "$(git remote)" ] && test $(git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py | wc -l) -ge 2 && test $(git log --oneline -- custom_components/luxtronik2_modbus_proxy/config_flow.py | wc -l) -ge 2 && test ! -e src && test ! -e Dockerfile && test ! -e mkdocs.yml && ! grep -q 'luxtronik2_modbus_proxy.main:cli' pyproject.toml && test -f README.md && echo PHASE_8_VERIFIED</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/unit/test_select.py tests/unit/test_number.py -x -q` exits 0
    - `git remote` output is empty
    - `git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py | wc -l` >= 2
    - `git log --oneline -- custom_components/luxtronik2_modbus_proxy/config_flow.py | wc -l` >= 2
    - None of `src`, `Dockerfile`, `docker-compose.yml`, `config.example.yaml`, `mkdocs.yml`, `contrib`, `docs` exist
    - `grep -q 'luxtronik2_modbus_proxy.main:cli' pyproject.toml` returns non-zero
    - `README.md` exists
    - Verification script exits 0
  </acceptance_criteria>
  <done>All 4 Phase 8 success criteria verified green. Pytest passes on surviving tests. SUMMARY captures tree listing and history proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source repo -> new working copy | Git history is copied; any secret/PII previously committed in the kept paths would survive into the new repo's history. |
| local filesystem -> (deferred) GitHub push | Phase 8 does NOT push. Any accidental secret only affects the local working copy; Phase 11 adds the pre-push secret scan gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | filter-repo path whitelist | mitigate | Whitelist is restrictive (only custom_components/, tests/, .github/, hacs.json, pyproject.toml, README*.md). Known-sensitive trees (src/, docs/, contrib/, .planning/) are dropped. Pre-push secret scan gate is Phase 11's responsibility. |
| T-08-02 | Tampering | Destructive git operation on wrong repo | mitigate | `git clone --no-local` to a NEW target path that must NOT pre-exist. `git remote remove origin` immediately after clone. Pre-flight check aborts if target exists. Filter-repo refuses to run without `--force` on non-fresh clones (pitfall 1). |
| T-08-03 | Information Disclosure | Placeholder README.md content | mitigate | Content is hardcoded in the plan (Task 3) — no IP addresses, no hostnames, no credentials. Executor copies the exact block. Acceptance criterion explicitly re-asserts CLAUDE.md rule. |
| T-08-04 | Information Disclosure | Commit messages leaked via git log | accept | Existing Phase 4-7 commit messages are assumed sanitized already (they were checked on commit via pre-commit hook). Filter-repo preserves messages verbatim. Re-auditing historical messages is out of scope for Phase 8. |
| T-08-05 | Denial of Service | Accidental push from new working copy | mitigate | `git remote remove origin` runs in Task 1 step 3. Verification command asserts `git remote` returns empty in Tasks 1, 2, and 4. Phase 8 task actions explicitly forbid adding a remote. |
| T-08-06 | Repudiation | Unclear what was extracted / when | mitigate | `.git/filter-repo/commit-map` is auto-generated by filter-repo and documents every old->new SHA mapping. SUMMARY records tree listing, commit counts, and pytest output. |
</threat_model>

<verification>
All of the following must be true at the end of Task 4:

1. `/home/dwolbeck/claude-code/luxtronik2-hass` exists and is a git repo with no remote origin.
2. `git log --oneline -- custom_components/luxtronik2_modbus_proxy/coordinator.py` shows >= 2 commits with Phase 4-7 messages.
3. `git blame custom_components/luxtronik2_modbus_proxy/coordinator.py | head -5` shows real SHAs (not `00000000`).
4. `find . -type f -not -path './.git/*'` shows NO `src/`, `Dockerfile`, `docker-compose.yml`, `config.example.yaml`, `mkdocs.yml`, `docs/`, `contrib/`.
5. `pyproject.toml` has no `[project.scripts]` section and no `main:cli` reference.
6. `README.md` exists with placeholder content; no real IPs, hostnames, or credentials.
7. Seven proxy-coupled tests are deleted; `tests/unit/test_select.py` and `tests/unit/test_number.py` survive.
8. `python3 -m pytest tests/unit/test_select.py tests/unit/test_number.py -x` exits 0.
9. The source repo at `/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy` is unmodified except for planning artifacts.
</verification>

<success_criteria>
- SPLIT-02: Git history preserved — verified by commit count >= 2 on coordinator.py and config_flow.py, plus non-zero blame SHAs.
- SPLIT-03: New repo contains only HA-relevant files — verified by forbidden-file absence check.
- Phase 8 SC-1: Working copy exists at target path with no remote origin.
- Phase 8 SC-2: History resolves via git log + git blame on coordinator.py and config_flow.py.
- Phase 8 SC-3: `find` confirms no proxy artifacts.
- Phase 8 SC-4: Trimmed pyproject.toml + placeholder README.md exist; pytest green.
</success_criteria>

<output>
After completion, create `/home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy/.planning/phases/08-new-repo-extraction-setup/08-01-SUMMARY.md` with:
- Target path and creation timestamp
- Commit counts for coordinator.py and config_flow.py
- Sample of 5 preserved commit messages (to prove SPLIT-02)
- Tree listing of the new working copy (first 60 files, sorted)
- pytest output (PASSED line)
- Note: SHAs are rewritten by filter-repo as expected; `.git/filter-repo/commit-map` documents old->new mapping
- Confirmation: no remote origin, source repo unmodified
</output>
