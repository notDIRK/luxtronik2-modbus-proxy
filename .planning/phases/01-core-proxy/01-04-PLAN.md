---
phase: 01-core-proxy
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - config.example.yaml
  - Dockerfile
  - docker-compose.yml
  - tests/integration/__init__.py
  - tests/integration/test_proxy_mock.py
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-03
  - PROTO-05

must_haves:
  truths:
    - "config.example.yaml contains all config options with placeholder values and explanatory comments"
    - "Docker image builds successfully from Dockerfile"
    - "docker compose up starts the proxy container"
    - "Integration test proves Modbus read/write flow through the full proxy stack with a mock Luxtronik server"
    - "No real IP addresses, hostnames, or credentials appear anywhere in committed files"
  artifacts:
    - path: "config.example.yaml"
      provides: "Example configuration with placeholders"
      contains: "192.168.x.x"
    - path: "Dockerfile"
      provides: "Docker container definition"
      contains: "python:3.12-slim"
    - path: "docker-compose.yml"
      provides: "Docker compose definition"
      contains: "config.example.yaml"
    - path: "tests/integration/test_proxy_mock.py"
      provides: "End-to-end smoke test with mock Luxtronik"
  key_links:
    - from: "Dockerfile"
      to: "pyproject.toml"
      via: "pip install"
      pattern: "pip install"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context"
      pattern: "build:"
    - from: "docker-compose.yml"
      to: "config.example.yaml"
      via: "volume mount"
      pattern: "volumes:"
---

<objective>
Create the Docker deployment artifacts, example configuration file, and an integration test that exercises the full proxy stack with a mock Luxtronik server.

Purpose: DEPLOY-01 and DEPLOY-03 require a working Docker container and documented example config. The integration test validates the end-to-end flow without real hardware.
Output: Buildable Docker image, config.example.yaml, and passing integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-proxy/01-RESEARCH.md
@.planning/phases/01-core-proxy/01-03-SUMMARY.md

<interfaces>
From src/luxtronik2_modbus_proxy/main.py:
```python
async def main(config_path: str | None = None) -> None: ...
def cli() -> None: ...
```

From pyproject.toml:
```toml
[project.scripts]
luxtronik2-modbus-proxy = "luxtronik2_modbus_proxy.main:cli"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker deployment and config.example.yaml</name>
  <files>
    config.example.yaml,
    Dockerfile,
    docker-compose.yml
  </files>
  <read_first>
    .planning/phases/01-core-proxy/01-RESEARCH.md (Pattern 6: pydantic-settings YAML Config, Pitfall 8: Docker SIGTERM)
    src/luxtronik2_modbus_proxy/config.py (ProxyConfig fields and defaults)
    CLAUDE.md (security constraints: no real IPs, placeholder values only)
    pyproject.toml (package name, dependencies)
  </read_first>
  <action>
    1. Create `config.example.yaml` with these exact contents:
       ```yaml
       # Luxtronik2 Modbus Proxy Configuration
       # Copy this file to config.yaml and adjust values for your setup.

       # Luxtronik controller connection
       # Replace with your heat pump's IP address
       luxtronik_host: "192.168.x.x"
       luxtronik_port: 8889

       # Modbus TCP server settings
       # Port the proxy listens on for Modbus TCP clients (evcc, Home Assistant, modpoll, etc.)
       modbus_port: 502
       # Bind address for the Modbus server
       # Use "0.0.0.0" to listen on all interfaces or a specific IP to restrict access
       bind_address: "0.0.0.0"

       # Polling settings
       # How often (in seconds) the proxy reads from the Luxtronik controller
       # Minimum: 10 seconds. Recommended: 30 seconds for coexistence with HA BenPru integration
       poll_interval: 30

       # Logging
       # Log level: DEBUG, INFO, WARNING, ERROR
       log_level: "INFO"

       # Write support
       # Set to true to allow Modbus clients to write registers (operating mode, setpoints)
       # WARNING: Writes modify your heat pump settings. Test carefully before enabling.
       enable_writes: false
       # Minimum seconds between writes to the same register (protects controller NAND flash)
       write_rate_limit: 60
       ```

    2. Create `Dockerfile`:
       ```dockerfile
       # Multi-stage build for luxtronik2-modbus-proxy
       # Base image: python:3.12-slim (manylinux wheels install instantly, no compilation)

       # --- Builder stage ---
       FROM python:3.12-slim AS builder

       WORKDIR /build
       COPY pyproject.toml .
       COPY src/ src/

       RUN pip install --no-cache-dir --prefix=/install .

       # --- Runtime stage ---
       FROM python:3.12-slim

       # Install tini for proper signal handling (SIGTERM forwarding, zombie reaping)
       RUN apt-get update && apt-get install -y --no-install-recommends tini \
           && rm -rf /var/lib/apt/lists/*

       # Create non-root user
       RUN useradd --create-home --uid 1000 proxy

       WORKDIR /app

       # Copy installed packages from builder
       COPY --from=builder /install /usr/local

       # Default config path (user mounts their config.yaml here)
       ENV LUXTRONIK_CONFIG_PATH=/app/config.yaml

       USER proxy

       EXPOSE 502

       # tini as PID 1 for signal handling
       ENTRYPOINT ["tini", "--"]
       CMD ["luxtronik2-modbus-proxy", "--config", "/app/config.yaml"]
       ```

    3. Create `docker-compose.yml`:
       ```yaml
       services:
         proxy:
           build: .
           ports:
             - "502:502"
           volumes:
             - ./config.yaml:/app/config.yaml:ro
           restart: unless-stopped
       ```
       Note: The compose file references `config.yaml` (not `config.example.yaml`). Users copy `config.example.yaml` to `config.yaml` and edit it.

    4. Verify `config.example.yaml` does NOT contain any real IP addresses (no `192.168.1.`, `10.0.`, `172.16.` through `172.31.` patterns) — only the placeholder `192.168.x.x`.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy && python -c "
import yaml, sys
with open('config.example.yaml') as f:
    cfg = yaml.safe_load(f)
assert cfg['luxtronik_host'] == '192.168.x.x', f'Expected placeholder IP, got {cfg[\"luxtronik_host\"]}'
assert cfg['poll_interval'] == 30
assert cfg['enable_writes'] == False
assert cfg['write_rate_limit'] == 60
print('config.example.yaml OK')
" && grep -q 'python:3.12-slim' Dockerfile && grep -q 'tini' Dockerfile && grep -q 'USER proxy' Dockerfile && echo "Dockerfile OK" && grep -q '502:502' docker-compose.yml && echo "docker-compose.yml OK"</automated>
  </verify>
  <acceptance_criteria>
    - config.example.yaml contains `luxtronik_host: "192.168.x.x"` (placeholder only)
    - config.example.yaml contains `poll_interval: 30`
    - config.example.yaml contains `enable_writes: false`
    - config.example.yaml contains `write_rate_limit: 60`
    - config.example.yaml does NOT contain any real IP like `192.168.1.` or `10.0.`
    - Dockerfile contains `FROM python:3.12-slim`
    - Dockerfile contains `tini`
    - Dockerfile contains `USER proxy`
    - Dockerfile contains `EXPOSE 502`
    - Dockerfile contains `luxtronik2-modbus-proxy`
    - docker-compose.yml contains `502:502`
    - docker-compose.yml contains `config.yaml:/app/config.yaml`
  </acceptance_criteria>
  <done>config.example.yaml has all options documented with placeholders. Dockerfile uses multi-stage build with python:3.12-slim, tini, non-root user. docker-compose.yml mounts config and exposes port 502.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test with mock Luxtronik server</name>
  <files>
    tests/integration/__init__.py,
    tests/integration/test_proxy_mock.py
  </files>
  <read_first>
    src/luxtronik2_modbus_proxy/main.py (main function, component wiring)
    src/luxtronik2_modbus_proxy/luxtronik_client.py (LuxtronikClient methods to mock)
    src/luxtronik2_modbus_proxy/register_map.py (RegisterMap addresses)
    src/luxtronik2_modbus_proxy/register_cache.py (RegisterCache, ProxyHoldingDataBlock)
    src/luxtronik2_modbus_proxy/config.py (ProxyConfig)
    src/luxtronik2_modbus_proxy/polling_engine.py (PollingEngine)
    src/luxtronik2_modbus_proxy/modbus_server.py (build_modbus_server)
  </read_first>
  <behavior>
    - Test 1: Start proxy with mock LuxtronikClient, read holding register 3 (HeatingMode) via pymodbus client, verify value matches what mock returned
    - Test 2: Start proxy with mock LuxtronikClient, read input register 10 (flow temperature) via pymodbus client, verify value matches mock
    - Test 3: Write value 2 (Party mode) to holding register 3 via pymodbus client with enable_writes=True, verify write appears in mock's write call
    - Test 4: Write invalid value 99 to holding register 3, verify Modbus exception response (code 3)
  </behavior>
  <action>
    1. Create `tests/integration/__init__.py` as empty file.

    2. Create `tests/integration/test_proxy_mock.py`:

    3. The test strategy: Build the full proxy component stack in-process but replace the LuxtronikClient with a mock that returns predetermined values. Use pymodbus's `ModbusTcpClient` (async) to connect to the proxy's Modbus server on a test port (e.g., 15502 to avoid requiring root).

    4. Create a `MockLuxtronikClient` class that replaces LuxtronikClient:
       - `async_read()` returns a mock object with `.parameters` and `.calculations` that return predetermined values when indexed
       - `async_write(param_writes)` records the writes for later assertion
       - `update_cache_from_read(lux, cache)` updates the cache with predetermined values

    5. Create a pytest fixture `proxy_stack` (async) that:
       - Creates a ProxyConfig with `luxtronik_host="192.168.x.x"`, `modbus_port=15502`, `poll_interval=10`, `enable_writes=True`
       - Creates RegisterMap, write_queue, RegisterCache, MockLuxtronikClient
       - Creates PollingEngine with the mock client
       - Creates Modbus server with `build_modbus_server(cache, config)`
       - Starts the server and runs ONE manual poll cycle (instead of run_forever) to populate the cache
       - Yields the components for testing
       - Tears down server after test

    6. Write test functions:
       - `test_read_holding_register`: Connect pymodbus AsyncModbusTcpClient to localhost:15502, read holding register address 3 (FC3), assert value equals mock's HeatingMode value
       - `test_read_input_register`: Read input register address 10 (FC4), assert value equals mock's flow temperature value
       - `test_write_valid_value`: Write value 2 to holding register 3 (FC6), verify no exception, verify mock received the write after a manual poll cycle drain
       - `test_write_invalid_value`: Write value 99 to holding register 3 (FC6), verify Modbus exception response with code 3

    7. Use `from pymodbus.client import AsyncModbusTcpClient` for the test client.

    8. Run `pytest tests/ -v` to verify all tests pass (unit + integration).
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy && pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - test_proxy_mock.py contains `class MockLuxtronikClient`
    - test_proxy_mock.py contains `AsyncModbusTcpClient`
    - test_proxy_mock.py contains `test_read_holding_register`
    - test_proxy_mock.py contains `test_read_input_register`
    - test_proxy_mock.py contains `test_write_valid_value`
    - test_proxy_mock.py contains `test_write_invalid_value`
    - test_proxy_mock.py contains `15502` (test port, not 502)
    - All tests pass (unit and integration)
  </acceptance_criteria>
  <done>Integration test proves full proxy stack works: Modbus client can read holding and input registers from the cache populated by mock Luxtronik data. Valid writes reach the mock client. Invalid writes are rejected with Modbus exception code 3. All unit and integration tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Docker build and proxy startup</name>
  <files>Dockerfile, docker-compose.yml, config.example.yaml</files>
  <action>Human verifies Docker image builds and proxy starts correctly. No automated action needed — this is a human verification checkpoint after Tasks 1 and 2 complete.</action>
  <what-built>Complete proxy application with Docker deployment. The proxy translates Luxtronik 2.0 binary protocol to Modbus TCP with validated writes, structured logging, and graceful shutdown.</what-built>
  <how-to-verify>
    1. Verify Docker builds: `docker build -t luxtronik2-modbus-proxy .`
       - Expected: Build completes successfully, image created
    2. Copy example config: `cp config.example.yaml config.yaml`
    3. Edit config.yaml: Set `luxtronik_host` to your heat pump IP (if available) or leave as placeholder
    4. If heat pump available: `docker compose up` — verify proxy starts and shows structured log output with polling cycle events
    5. If no heat pump: `docker compose up` — verify proxy starts, attempts connection, logs connection error, and continues retrying (does not crash)
    6. Verify no real IPs in repo: `git diff --cached` and `git log --oneline` should show no private data
  </how-to-verify>
  <verify>Human checkpoint — see how-to-verify steps above</verify>
  <done>Docker image builds successfully. Proxy starts in container and either connects to heat pump or retries gracefully on connection error. No real IPs in repository.</done>
  <resume-signal>Type "approved" if Docker builds and proxy starts correctly, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host -> container | Container runs as non-root user (UID 1000) |
| config.yaml -> proxy | User-supplied configuration mounted into container |
| Test client -> test server | Integration test exercises write validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | config.example.yaml | mitigate | Uses placeholder `192.168.x.x` only; pre-commit hook blocks real IPs; config.yaml is gitignored |
| T-04-02 | Elevation of Privilege | Dockerfile | mitigate | Multi-stage build; runtime runs as non-root `proxy` user (UID 1000); no capabilities added |
| T-04-03 | Denial of Service | Docker container | mitigate | tini as PID 1 handles SIGTERM properly; `restart: unless-stopped` in compose for crash recovery |
| T-04-04 | Tampering | test_proxy_mock.py | accept | Test code only; mock validates that real validation logic works; no security impact |
</threat_model>

<verification>
- `config.example.yaml` parses as valid YAML with all expected keys
- `Dockerfile` contains multi-stage build, tini, non-root user
- `docker-compose.yml` mounts config and exposes port
- `pytest tests/ -v` passes all tests (unit + integration)
- `grep -r "192\.168\.[0-9]" --include="*.py" --include="*.yaml" --include="*.yml" .` returns only test files with mock IPs (192.168.x.x pattern is allowed)
- Docker image builds successfully (verified by human in checkpoint)
</verification>

<success_criteria>
Docker image builds from Dockerfile. config.example.yaml documents all options with safe placeholder values. Integration test validates the full proxy stack with mock Luxtronik. All unit and integration tests pass. Human verifies Docker build and startup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-proxy/01-04-SUMMARY.md`
</output>
