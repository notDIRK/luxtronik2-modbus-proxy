---
phase: 01-core-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/luxtronik2_modbus_proxy/__init__.py
  - src/luxtronik2_modbus_proxy/config.py
  - src/luxtronik2_modbus_proxy/logging_config.py
  - src/luxtronik2_modbus_proxy/register_definitions/__init__.py
  - src/luxtronik2_modbus_proxy/register_definitions/parameters.py
  - src/luxtronik2_modbus_proxy/register_definitions/calculations.py
  - src/luxtronik2_modbus_proxy/register_map.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/unit/test_config.py
  - tests/unit/test_register_map.py
autonomous: true
requirements:
  - REG-01
  - OBS-01

must_haves:
  truths:
    - "Project installs cleanly with pip install -e ."
    - "Config loads from YAML with env var overrides"
    - "structlog produces JSON in non-TTY and colored output in TTY"
    - "Register map resolves luxtronik parameter IDs to Modbus addresses"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, tool config"
      contains: "luxtronik2-modbus-proxy"
    - path: "src/luxtronik2_modbus_proxy/config.py"
      provides: "ProxyConfig pydantic-settings model"
      exports: ["ProxyConfig"]
    - path: "src/luxtronik2_modbus_proxy/logging_config.py"
      provides: "structlog configuration"
      exports: ["configure_logging"]
    - path: "src/luxtronik2_modbus_proxy/register_map.py"
      provides: "Register map with address lookup"
      exports: ["RegisterMap", "RegisterEntry"]
    - path: "src/luxtronik2_modbus_proxy/register_definitions/parameters.py"
      provides: "Curated parameter definitions"
      contains: "HOLDING_REGISTERS"
    - path: "src/luxtronik2_modbus_proxy/register_definitions/calculations.py"
      provides: "Curated calculation definitions"
      contains: "INPUT_REGISTERS"
  key_links:
    - from: "src/luxtronik2_modbus_proxy/register_map.py"
      to: "register_definitions/parameters.py"
      via: "import HOLDING_REGISTERS"
      pattern: "from.*register_definitions.*import"
    - from: "src/luxtronik2_modbus_proxy/config.py"
      to: "pydantic_settings"
      via: "BaseSettings subclass"
      pattern: "class ProxyConfig.*BaseSettings"
---

<objective>
Scaffold the project and implement the foundational data layer: project configuration, structured logging, curated register definitions, and the register map that translates between Luxtronik parameter IDs and Modbus wire addresses.

Purpose: Every subsequent plan depends on ProxyConfig, RegisterMap, and the logging setup. Building these first prevents scavenger hunts for types and conventions.
Output: Installable Python package with config, logging, register map, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-proxy/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, config, and logging</name>
  <files>
    pyproject.toml,
    src/luxtronik2_modbus_proxy/__init__.py,
    src/luxtronik2_modbus_proxy/config.py,
    src/luxtronik2_modbus_proxy/logging_config.py,
    tests/__init__.py,
    tests/unit/__init__.py,
    tests/unit/test_config.py
  </files>
  <read_first>
    .planning/phases/01-core-proxy/01-RESEARCH.md (Pattern 6: pydantic-settings YAML Config, Pattern 7: structlog Configuration)
    CLAUDE.md (code standards, security constraints)
  </read_first>
  <behavior>
    - Test 1: ProxyConfig loads luxtronik_host from a YAML file at a specified path
    - Test 2: ProxyConfig uses defaults for poll_interval (30), luxtronik_port (8889), modbus_port (502)
    - Test 3: ProxyConfig rejects poll_interval below 10 with ValidationError
    - Test 4: ProxyConfig env vars with LUXTRONIK_ prefix override YAML values
    - Test 5: configure_logging("DEBUG") succeeds without error
    - Test 6: configure_logging("INFO") succeeds without error
  </behavior>
  <action>
    1. Create `pyproject.toml` with:
       - `[project]` name="luxtronik2-modbus-proxy", requires-python=">=3.10"
       - Dependencies: `luxtronik==0.3.14`, `pymodbus==3.12.1`, `pydantic>=2.12,<3`, `pydantic-settings>=2.13,<3`, `structlog>=25.0`
       - `[project.optional-dependencies]` dev: `pytest>=9.0`, `pytest-asyncio>=1.3`, `pytest-cov`, `ruff`, `mypy`
       - `[tool.pytest.ini_options]` asyncio_mode="auto", testpaths=["tests"]
       - `[tool.ruff]` target-version="py310", line-length=100
       - `[tool.ruff.lint]` select=["E","F","W","I","D","UP","B","SIM","RUF"], pydocstyle convention="google"
       - `[tool.mypy]` strict=true, python_version="3.10"

    2. Create `src/luxtronik2_modbus_proxy/__init__.py` with `__version__ = "0.1.0"`.

    3. Create `src/luxtronik2_modbus_proxy/config.py` with class `ProxyConfig(BaseSettings)`:
       - Fields (all with Field() and description):
         - `luxtronik_host: str` (required, no default)
         - `luxtronik_port: int = 8889`
         - `modbus_port: int = 502`
         - `bind_address: str = "0.0.0.0"`
         - `poll_interval: int = Field(30, ge=10, description="Polling interval in seconds (min 10)")`
         - `log_level: str = "INFO"`
         - `enable_writes: bool = False` (safety default: writes disabled unless explicitly enabled)
         - `write_rate_limit: int = Field(60, ge=10, description="Min seconds between writes to same register")`
       - `model_config` with `yaml_file` default and `env_prefix="LUXTRONIK_"`
       - `settings_customise_sources` classmethod returning `(init_settings, YamlConfigSettingsSource(settings_cls), env_settings)` exactly as in research Pattern 6
       - Add a `load_config(yaml_path: str | None = None) -> ProxyConfig` convenience function that accepts an optional path override for the YAML file

    4. Create `src/luxtronik2_modbus_proxy/logging_config.py` with function `configure_logging(log_level: str) -> None`:
       - Import structlog, logging, os
       - Detect TTY via `os.isatty(1)`
       - Use `structlog.dev.ConsoleRenderer()` for TTY, `structlog.processors.JSONRenderer()` for non-TTY
       - Processors: `merge_contextvars`, `add_log_level`, `TimeStamper(fmt="iso")`, renderer
       - `wrapper_class=structlog.make_filtering_bound_logger(getattr(logging, log_level.upper(), logging.INFO))`
       - `logger_factory=structlog.PrintLoggerFactory()`

    5. Create `tests/__init__.py` and `tests/unit/__init__.py` as empty files.

    6. Create `tests/unit/test_config.py`:
       - Use `tmp_path` fixture to write temporary YAML config files
       - Test loading from YAML with `luxtronik_host: "192.168.x.x"`
       - Test defaults for optional fields
       - Test validation error for `poll_interval: 5` (below minimum 10)
       - Test env var override with `monkeypatch.setenv("LUXTRONIK_POLL_INTERVAL", "60")`
       - Test `configure_logging` calls (just verify no exception)

    7. Run `pip install -e ".[dev]"` to install the package in development mode, then run `pytest tests/unit/test_config.py -v` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy && pip install -e ".[dev]" && pytest tests/unit/test_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "luxtronik2-modbus-proxy"`
    - pyproject.toml contains `"luxtronik==0.3.14"` in dependencies
    - pyproject.toml contains `"pymodbus==3.12.1"` in dependencies
    - pyproject.toml contains `asyncio_mode = "auto"`
    - config.py contains `class ProxyConfig(BaseSettings):`
    - config.py contains `luxtronik_host: str`
    - config.py contains `poll_interval: int = Field(30, ge=10`
    - config.py contains `enable_writes: bool`
    - config.py contains `env_prefix`
    - config.py contains `YamlConfigSettingsSource`
    - logging_config.py contains `def configure_logging(log_level: str) -> None:`
    - logging_config.py contains `structlog.configure(`
    - logging_config.py contains `JSONRenderer`
    - logging_config.py contains `ConsoleRenderer`
    - test_config.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>ProxyConfig loads from YAML with env var overrides, validates poll_interval >= 10, and all config tests pass. structlog configured with JSON/Console renderer switching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register definitions and register map</name>
  <files>
    src/luxtronik2_modbus_proxy/register_definitions/__init__.py,
    src/luxtronik2_modbus_proxy/register_definitions/parameters.py,
    src/luxtronik2_modbus_proxy/register_definitions/calculations.py,
    src/luxtronik2_modbus_proxy/register_map.py,
    tests/unit/test_register_map.py
  </files>
  <read_first>
    .planning/phases/01-core-proxy/01-RESEARCH.md (Code Examples: Full Curated Register Map, Pattern 2: Register Address Convention)
    src/luxtronik2_modbus_proxy/config.py (ProxyConfig type for imports)
  </read_first>
  <behavior>
    - Test 1: HOLDING_REGISTERS dict has entries at keys 3, 4, 105 with correct luxtronik IDs
    - Test 2: INPUT_REGISTERS dict has entries at keys 10, 11, 15, 17, 80, 257 with correct luxtronik IDs
    - Test 3: RegisterMap.get_holding_entry(3) returns RegisterEntry with luxtronik_id="ID_Ba_Hz_akt", writable=True
    - Test 4: RegisterMap.get_input_entry(10) returns RegisterEntry with luxtronik_id="ID_WEB_Temperatur_TVL"
    - Test 5: RegisterMap.get_holding_entry(9999) returns None (unmapped address)
    - Test 6: RegisterMap.is_writable(3) returns True; RegisterMap.is_writable(9999) returns False
    - Test 7: RegisterMap.validate_write_value(3, 0) returns True (HeatingMode 0=Automatic is valid)
    - Test 8: RegisterMap.validate_write_value(3, 99) returns False (HeatingMode 99 is out of range)
    - Test 9: RegisterMap.validate_write_value(105, 450) returns True (45.0C is valid DHW setpoint)
    - Test 10: RegisterMap.validate_write_value(105, 100) returns False (10.0C is below min 30C)
    - Test 11: RegisterMap.validate_write_value(105, 700) returns False (70.0C is above max 65C)
    - Test 12: RegisterMap.holding_block_size returns 1200; RegisterMap.input_block_size returns 260
  </behavior>
  <action>
    1. Create `src/luxtronik2_modbus_proxy/register_definitions/__init__.py` as empty file.

    2. Create `src/luxtronik2_modbus_proxy/register_definitions/parameters.py`:
       - Define a `ParameterDef` dataclass with fields: `luxtronik_id: str`, `name: str`, `data_type: str`, `writable: bool`, `allowed_values: list[int] | None = None`, `min_value: int | None = None`, `max_value: int | None = None`
       - Define `HOLDING_REGISTERS: dict[int, ParameterDef]` with these exact entries:
         - `3: ParameterDef("ID_Ba_Hz_akt", "Heating circuit mode", "HeatingMode", True, allowed_values=[0, 1, 2, 3, 4])`
           Comment: `# {0: Automatic, 1: Second heatsource, 2: Party, 3: Holidays, 4: Off}`
         - `4: ParameterDef("ID_Ba_Bw_akt", "Hot water mode", "HotWaterMode", True, allowed_values=[0, 1, 2, 3, 4])`
           Comment: `# {0: Automatic, 1: Second heatsource, 2: Party, 3: Holidays, 4: Off}`
         - `105: ParameterDef("ID_Soll_BWS_akt", "Hot water setpoint (raw, C*10)", "Celsius", True, min_value=300, max_value=650)`
           Comment: `# 300=30.0C min, 650=65.0C max; raw value is Celsius * 10`

    3. Create `src/luxtronik2_modbus_proxy/register_definitions/calculations.py`:
       - Define a `CalculationDef` dataclass with fields: `luxtronik_id: str`, `name: str`, `data_type: str`
       - Define `INPUT_REGISTERS: dict[int, CalculationDef]` with these exact entries:
         - `10: CalculationDef("ID_WEB_Temperatur_TVL", "Flow temperature (raw, C*10)", "Celsius")`
         - `11: CalculationDef("ID_WEB_Temperatur_TRL", "Return temperature (raw, C*10)", "Celsius")`
         - `15: CalculationDef("ID_WEB_Temperatur_TA", "Outside temperature (raw, C*10)", "Celsius")`
         - `17: CalculationDef("ID_WEB_Temperatur_TBW", "Hot water temperature (raw, C*10)", "Celsius")`
         - `80: CalculationDef("ID_WEB_WP_BZ_akt", "Operating mode", "OperationMode")`
           Comment: `# {0: heating, 1: hot water, 2: swimming pool/solar, 3: evu, 4: defrost, 5: no request}`
         - `257: CalculationDef("Heat_Output", "Heat output (W)", "Power")`

    4. Create `src/luxtronik2_modbus_proxy/register_map.py`:
       - Import `ParameterDef` from `register_definitions.parameters` and `CalculationDef` from `register_definitions.calculations`
       - Define `RegisterEntry` dataclass: `address: int`, `luxtronik_id: str`, `name: str`, `data_type: str`, `writable: bool = False`, `allowed_values: list[int] | None = None`, `min_value: int | None = None`, `max_value: int | None = None`
       - Define `RegisterMap` class:
         - `HOLDING_BLOCK_SIZE: int = 1200` (class constant, covers parameter indices 0-1125 + margin)
         - `INPUT_BLOCK_SIZE: int = 260` (class constant, covers calculation indices 0-257 + margin)
         - `__init__()` builds internal dicts `_holding: dict[int, RegisterEntry]` and `_input: dict[int, RegisterEntry]` from the imported register definitions
         - `holding_block_size` property returns `HOLDING_BLOCK_SIZE`
         - `input_block_size` property returns `INPUT_BLOCK_SIZE`
         - `get_holding_entry(address: int) -> RegisterEntry | None`
         - `get_input_entry(address: int) -> RegisterEntry | None`
         - `is_writable(address: int) -> bool` — returns True only if holding entry exists and is writable
         - `validate_write_value(address: int, value: int) -> bool` — checks `allowed_values` or `min_value/max_value` range; returns False if address is not writable or value is out of range
         - `all_holding_addresses() -> list[int]` — returns sorted list of mapped holding register addresses
         - `all_input_addresses() -> list[int]` — returns sorted list of mapped input register addresses

    5. Create `tests/unit/test_register_map.py` with all behavior tests listed above.

    6. Run `pytest tests/unit/test_register_map.py -v`.
  </action>
  <verify>
    <automated>cd /home/dwolbeck/claude-code/PUBLIC-luxtronik2-modbus-proxy && pytest tests/unit/test_register_map.py -v</automated>
  </verify>
  <acceptance_criteria>
    - parameters.py contains `HOLDING_REGISTERS: dict[int, ParameterDef]`
    - parameters.py contains `"ID_Ba_Hz_akt"` at key 3
    - parameters.py contains `"ID_Ba_Bw_akt"` at key 4
    - parameters.py contains `"ID_Soll_BWS_akt"` at key 105
    - parameters.py contains `allowed_values=[0, 1, 2, 3, 4]` for HeatingMode
    - parameters.py contains `min_value=300, max_value=650` for DHW setpoint
    - calculations.py contains `INPUT_REGISTERS: dict[int, CalculationDef]`
    - calculations.py contains `"ID_WEB_Temperatur_TVL"` at key 10
    - calculations.py contains `"Heat_Output"` at key 257
    - register_map.py contains `class RegisterMap:`
    - register_map.py contains `HOLDING_BLOCK_SIZE: int = 1200`
    - register_map.py contains `INPUT_BLOCK_SIZE: int = 260`
    - register_map.py contains `def validate_write_value(`
    - test_register_map.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>Register definitions for all Phase 1 curated registers exist with validation ranges. RegisterMap provides address lookup, writability checks, and write value validation. All register map tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file -> ProxyConfig | User-supplied YAML parsed at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | config.py | mitigate | Validate all config values with pydantic Field constraints (ge=10 for poll_interval, etc.); reject invalid config at startup before any network activity |
| T-01-02 | Information Disclosure | register_definitions | mitigate | Only curated register set (~9 registers) exposed in Phase 1 defaults; no full parameter dump |
| T-01-03 | Information Disclosure | config.yaml | mitigate | config.yaml is gitignored; config.example.yaml uses placeholder IPs only; pre-commit hook blocks real IPs |
</threat_model>

<verification>
- `pip install -e ".[dev]"` succeeds
- `pytest tests/unit/ -v` passes all tests
- `python -c "from luxtronik2_modbus_proxy.config import ProxyConfig"` succeeds
- `python -c "from luxtronik2_modbus_proxy.register_map import RegisterMap"` succeeds
- `python -c "from luxtronik2_modbus_proxy.logging_config import configure_logging"` succeeds
</verification>

<success_criteria>
Project installs cleanly, ProxyConfig validates YAML+env config, structlog is configured, register map resolves all Phase 1 curated registers with write validation, and all unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-proxy/01-01-SUMMARY.md`
</output>
