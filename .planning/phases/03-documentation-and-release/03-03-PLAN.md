---
phase: 03-documentation-and-release
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - docs/de/quickstart.md
  - docs/de/user-guide.md
  - docs/de/systemd.md
  - docs/de/evcc-integration.md
  - docs/de/ha-coexistence.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "German-speaking user can follow user-guide.md (DE) to install and configure the proxy"
    - "Every EN docs file has a corresponding DE translation with mirrored structure"
    - "DE translations use the same code examples and commands as EN (code is not translated)"
  artifacts:
    - path: "docs/de/quickstart.md"
      provides: "German developer quickstart"
      contains: "# Schnellstart"
    - path: "docs/de/user-guide.md"
      provides: "German end-user guide"
      contains: "# Benutzerhandbuch"
    - path: "docs/de/systemd.md"
      provides: "German systemd guide"
      contains: "# systemd-Dienst"
    - path: "docs/de/evcc-integration.md"
      provides: "German evcc integration guide"
      contains: "# evcc-Integrationsanleitung"
    - path: "docs/de/ha-coexistence.md"
      provides: "German HA coexistence guide"
      contains: "# Parallelbetrieb mit Home Assistant"
  key_links:
    - from: "docs/de/user-guide.md"
      to: "docs/de/evcc-integration.md"
      via: "internal link"
      pattern: "evcc-integration.md"
    - from: "docs/de/user-guide.md"
      to: "docs/de/systemd.md"
      via: "internal link"
      pattern: "systemd.md"
---

<objective>
Translate all English documentation to German, creating the complete DE language track for the MkDocs site.

Purpose: Enables German-speaking heat pump owners (the primary target audience) to install and use the proxy in their native language, per D-03.
Output: Five German documentation files in docs/de/ mirroring the EN structure exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-documentation-and-release/03-CONTEXT.md

@docs/en/quickstart.md
@docs/en/user-guide.md
@docs/en/systemd.md
@docs/en/evcc-integration.md
@docs/en/ha-coexistence.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate quickstart, user-guide, and systemd guides to German</name>
  <files>docs/de/quickstart.md, docs/de/user-guide.md, docs/de/systemd.md</files>
  <read_first>
    docs/en/quickstart.md
    docs/en/user-guide.md
    docs/en/systemd.md
  </read_first>
  <action>
Translate the three new English guides to German (per D-03: DE mirrors EN structure and headings exactly).

Translation rules:
- Section headings are translated to German (e.g., "## Prerequisites" -> "## Voraussetzungen")
- All code blocks, commands, file paths, and config keys remain in English (they are code, not prose)
- Technical terms that are commonly used in German IT context stay in English: Docker, Modbus, systemd, Home Assistant, evcc, pip, git, journalctl
- IP address placeholders remain `192.168.x.x`
- Tone: clear, direct, accessible to non-technical users (target: German heat pump owner)

`docs/de/quickstart.md`:
- H1: `# Schnellstart`
- Mirror all sections from EN quickstart.md
- Key heading translations: "Voraussetzungen", "Klonen und Installieren", "Konfigurieren", "Starten", "Funktionstest", "Tests ausfuehren", "Docker-Alternative", "Naechste Schritte"

`docs/de/user-guide.md`:
- H1: `# Benutzerhandbuch`
- Key heading translations: "Was macht dieser Proxy?", "Voraussetzungen", "Installation (Docker)", "Funktionstest", "evcc verbinden", "Home Assistant verbinden", "Fehlerbehebung", "Alternative: systemd-Dienst"
- Config field descriptions translated but field names stay English (they are YAML keys)

`docs/de/systemd.md`:
- H1: `# systemd-Dienst`
- Key heading translations: "Wann systemd verwenden?", "Voraussetzungen", "Proxy installieren", "Systembenutzer erstellen", "Konfiguration installieren", "Service-Datei installieren", "Status pruefen", "Logs anzeigen", "Umgebungsvariablen", "Fehlerbehebung"

Run `mkdir -p docs/de` before writing files.
  </action>
  <verify>
    <automated>test -f docs/de/quickstart.md && test -f docs/de/user-guide.md && test -f docs/de/systemd.md && grep -q "Schnellstart" docs/de/quickstart.md && grep -q "Benutzerhandbuch" docs/de/user-guide.md && grep -q "systemd" docs/de/systemd.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/de/quickstart.md exists and contains `# Schnellstart`
    - docs/de/quickstart.md contains `pip install -e` (code unchanged)
    - docs/de/quickstart.md contains `## Voraussetzungen`
    - docs/de/user-guide.md exists and contains `# Benutzerhandbuch`
    - docs/de/user-guide.md contains `docker compose up` (code unchanged)
    - docs/de/user-guide.md contains `luxtronik_host` (config key unchanged)
    - docs/de/systemd.md exists and contains `# systemd-Dienst`
    - docs/de/systemd.md contains `systemctl enable` (code unchanged)
    - No file contains real IP addresses
    - All three files have the same number of H2 sections as their EN counterparts
  </acceptance_criteria>
  <done>Three German guide translations exist with mirrored structure, translated prose, and untranslated code blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Translate existing evcc and HA guides to German</name>
  <files>docs/de/evcc-integration.md, docs/de/ha-coexistence.md</files>
  <read_first>
    docs/en/evcc-integration.md
    docs/en/ha-coexistence.md
  </read_first>
  <action>
Translate the two existing Phase 2 guides to German (per D-01, D-03).

Same translation rules as Task 1: German prose, English code/config/commands.

`docs/de/evcc-integration.md`:
- H1: `# evcc-Integrationsanleitung`
- Mirror the complete structure of `docs/en/evcc-integration.md`
- YAML configuration snippets remain exactly as in EN (they are evcc config, not prose)
- Modbus register addresses and field names stay English
- Translate explanatory text around the code blocks

`docs/de/ha-coexistence.md`:
- H1: `# Parallelbetrieb mit Home Assistant`
- Mirror the complete structure of `docs/en/ha-coexistence.md`
- Technical terms stay English: Home Assistant, BenPru, HACS, Luxtronik
- Translate explanatory text, keep ASCII diagrams and code blocks unchanged
  </action>
  <verify>
    <automated>test -f docs/de/evcc-integration.md && test -f docs/de/ha-coexistence.md && grep -q "evcc" docs/de/evcc-integration.md && grep -q "Home Assistant" docs/de/ha-coexistence.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/de/evcc-integration.md exists and contains `# evcc-Integrationsanleitung`
    - docs/de/evcc-integration.md contains the evcc YAML configuration snippet (unchanged from EN)
    - docs/de/ha-coexistence.md exists and contains `# Parallelbetrieb mit Home Assistant`
    - docs/de/ha-coexistence.md contains the single-connection explanation
    - Both files have German prose surrounding English code blocks
    - No file contains real IP addresses
  </acceptance_criteria>
  <done>Complete German translations of all five documentation files exist in docs/de/, providing full DE language track for MkDocs site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | German translations must not introduce sensitive data absent from EN originals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | docs/de/*.md | mitigate | Mirror EN placeholders exactly (192.168.x.x); pre-commit hook applies to all files |
</threat_model>

<verification>
1. All five docs/de/ files exist
2. `ls docs/de/ | wc -l` returns 5 (or 6 counting index.md from Plan 01)
3. `grep -rn '[0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+' docs/de/` returns only 192.168.x.x patterns
4. Each DE file has the same number of H2 headings as its EN counterpart
</verification>

<success_criteria>
- All five English docs have German counterparts with mirrored structure
- German translations read naturally for a non-technical German speaker
- Code blocks, commands, and config keys are identical between EN and DE
- MkDocs language switcher can toggle between complete EN and DE content
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-and-release/03-03-SUMMARY.md`
</output>
