---
phase: 03-documentation-and-release
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - README.md
  - README.de.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "README.md gives an accurate project overview with architecture diagram"
    - "README.de.md mirrors README.md in German with an English link at top"
    - "A new visitor understands what the project does within 30 seconds of reading the README"
  artifacts:
    - path: "README.md"
      provides: "English project overview with badges, diagram, quick-start"
      contains: "luxtronik2-modbus-proxy"
    - path: "README.de.md"
      provides: "German project overview"
      contains: "luxtronik2-modbus-proxy"
  key_links:
    - from: "README.md"
      to: "docs/en/quickstart.md"
      via: "documentation link"
      pattern: "docs/en/quickstart.md"
    - from: "README.md"
      to: "docs/en/user-guide.md"
      via: "documentation link"
      pattern: "docs/en/user-guide.md"
    - from: "README.de.md"
      to: "README.md"
      via: "Read in English link"
      pattern: "README.md"
---

<objective>
Create polished README.md (EN) and README.de.md (DE) with project badges, ASCII architecture diagram, 3-line Docker quick-start, and links to full documentation.

Purpose: The README is the first thing visitors see on GitHub. It must clearly communicate what the project does, who it is for, and how to get started.
Output: Two README files at project root summarizing the completed v1 feature set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-documentation-and-release/03-CONTEXT.md

@pyproject.toml
@config.example.yaml
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with badges, architecture diagram, and quick-start</name>
  <files>README.md</files>
  <read_first>
    pyproject.toml
    config.example.yaml
    docker-compose.yml
    docs/en/quickstart.md
    docs/en/user-guide.md
  </read_first>
  <action>
Create `README.md` at project root (per D-04, D-06). This is a compact README — not a full guide.

Structure (per D-04):

1. **Badge block** (first line, no heading above):
   ```markdown
   ![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue)
   ![License: MIT](https://img.shields.io/badge/license-MIT-green)
   ![Docker](https://img.shields.io/badge/docker-supported-blue)
   ```

2. **Title**: `# luxtronik2-modbus-proxy`

3. **Language link**: `> Auf Deutsch lesen: [README.de.md](README.de.md)`

4. **One-paragraph description**: Modbus TCP proxy for Luxtronik 2.0 heat pump controllers. Translates the proprietary Luxtronik binary protocol (port 8889) to standard Modbus TCP (port 502), enabling evcc, Home Assistant, and other Modbus-capable tools to control older heat pumps from Alpha Innotec, Novelan, Buderus, Nibe, Roth, Elco, and Wolf that lack native Modbus support.

5. **ASCII architecture diagram** (per D-06):
   ```
   ## Architecture

   ```
   ┌─────────────────────┐         ┌──────────────────────────┐
   │  Luxtronik 2.0      │         │  luxtronik2-modbus-proxy │
   │  Heat Pump           │ <────> │                          │ <── evcc
   │  Controller          │         │  Modbus TCP Server       │
   │  port 8889          │         │  port 502               │ <── Home Assistant
   └─────────────────────┘         └──────────────────────────┘
     proprietary protocol            standard Modbus TCP

   Connect → Read/Write → Disconnect (coexists with HA integration)
   ```
   ```

6. **3-line Docker quick-start** (per D-04):
   ```markdown
   ## Quick Start

   ```bash
   cp config.example.yaml config.yaml
   # Edit config.yaml: set luxtronik_host to your heat pump's IP
   docker compose up -d
   ```
   ```

7. **Features** — bullet list:
   - Modbus TCP server supporting FC3, FC4, FC6, FC16
   - Connect-and-release polling (coexists with HA BenPru integration)
   - SG-ready virtual register for evcc heat pump control
   - 1,126 Luxtronik parameters selectable by name
   - Configurable via YAML with environment variable overrides
   - Docker and systemd deployment

8. **Documentation** — links section:
   ```markdown
   ## Documentation

   - [Developer Quick Start](docs/en/quickstart.md) — build and run from source
   - [User Guide](docs/en/user-guide.md) — install and configure (Docker)
   - [systemd Service](docs/en/systemd.md) — Linux service deployment
   - [evcc Integration](docs/en/evcc-integration.md) — heat pump control via evcc
   - [HA Coexistence](docs/en/ha-coexistence.md) — running alongside Home Assistant
   ```

9. **License**: `## License` — MIT. See [LICENSE](LICENSE).

The pyproject.toml `readme` field already points to `README.md` — do NOT change it (per D-05).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "shields.io" README.md && grep -q "luxtronik2-modbus-proxy" README.md && grep -q "Architecture" README.md && grep -q "docker compose up" README.md && grep -q "docs/en/quickstart.md" README.md && grep -q "README.de.md" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - Contains three shields.io badge images (Python, License, Docker)
    - Contains `# luxtronik2-modbus-proxy` heading
    - Contains link to README.de.md
    - Contains `## Architecture` with ASCII diagram showing port 8889, port 502, evcc, Home Assistant
    - Contains `## Quick Start` with exactly the 3-line Docker commands
    - Contains `## Documentation` with links to all five docs/en/ guides
    - Contains `## License` section
    - Does NOT contain real IP addresses
    - pyproject.toml `readme` field is NOT modified
  </acceptance_criteria>
  <done>README.md is compact, informative, and links to all documentation. Visitors understand the project within 30 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.de.md mirroring English README</name>
  <files>README.de.md</files>
  <read_first>
    README.md
  </read_first>
  <action>
Create `README.de.md` at project root (per D-05). Mirrors README.md in German.

Key differences from README.md:
1. Add "Read in English" link at the very top: `> Read in English: [README.md](README.md)`
2. Same badge block (badges are language-neutral)
3. Same `# luxtronik2-modbus-proxy` title (project name is not translated)
4. Description paragraph translated to German
5. Architecture diagram: same ASCII art (code is not translated), but surrounding text in German
6. Quick Start section: same commands, German explanatory text
7. Features list: translated to German
8. Documentation links: point to `docs/de/` versions:
   ```markdown
   ## Dokumentation

   - [Schnellstart](docs/de/quickstart.md) — Projekt klonen und starten
   - [Benutzerhandbuch](docs/de/user-guide.md) — Installation und Konfiguration (Docker)
   - [systemd-Dienst](docs/de/systemd.md) — Linux-Service-Installation
   - [evcc-Integration](docs/de/evcc-integration.md) — Waermepumpensteuerung mit evcc
   - [Parallelbetrieb mit HA](docs/de/ha-coexistence.md) — Betrieb neben Home Assistant
   ```
9. License section: same content, German heading "## Lizenz"

Translation rules: same as Plan 03 — German prose, English code/commands/config.
  </action>
  <verify>
    <automated>test -f README.de.md && grep -q "README.md" README.de.md && grep -q "shields.io" README.de.md && grep -q "docs/de/" README.de.md && grep -q "docker compose up" README.de.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.de.md exists at project root
    - First non-empty line contains link to README.md ("Read in English")
    - Contains same three shields.io badge images as README.md
    - Contains `# luxtronik2-modbus-proxy` heading
    - Contains `## Dokumentation` with links to docs/de/ files
    - Contains `docker compose up -d` command (code unchanged)
    - Contains `## Lizenz` section
    - Does NOT contain real IP addresses
    - pyproject.toml is NOT modified (readme field stays pointing to README.md per D-05)
  </acceptance_criteria>
  <done>README.de.md mirrors README.md in German with docs/de/ links and EN link at top. German-speaking visitors get native-language project overview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | README files are public-facing; must not leak sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | README.md, README.de.md | mitigate | All examples use 192.168.x.x placeholder; pre-commit hook enforces; no credentials in badges or links |
</threat_model>

<verification>
1. `test -f README.md && test -f README.de.md` exits 0
2. `grep -c "shields.io" README.md` returns 3
3. `grep -c "docs/de/" README.de.md` returns 5 (one per guide link)
4. `grep -c "docs/en/" README.md` returns 5 (one per guide link)
5. `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['readme']=='README.md'"` exits 0
</verification>

<success_criteria>
- README.md is compact with badges, diagram, 3-line quick-start, and doc links
- README.de.md mirrors README.md in German with EN link at top
- Both files accurately describe the v1 shipped feature set
- pyproject.toml readme field unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-and-release/03-04-SUMMARY.md`
</output>
